# Chat Overhaul — Original Phase Plan & Status Matrix

Tracking document for the chat suite overhaul. Contains the **original matrix
phase plan** (Fluxer-inspired chat for Dental Diamond Link) verbatim, overlaid
with current build status, per-phase progress, and next steps.

> Last updated: 2026-09-07 · Latest commits (all on `master`): **Phase 5 push notifications — complete + live-tested working**: VAPID keys, `web-push`, `push_subscriptions` table, 6 API routes (`vapid-public-key`/`subscribe`/`unsubscribe`/`test`/`send`/`webhook`), WhatsApp-style per-thread aggregated SW notifications `6f2e9cf`, never-silent always-visible push handler `8e8c452`, cold-boot sign-in fix `f2bf1f2`, notification deep-link re-render fix `fbcac20`, avatar/actions + subscribe-chatter fix `6dfebbc`, pg_net `headers` 4th-arg fix `66e83e4`; device-verified on Chromebook PWA / mobile PWA / fully-closed app; **Phase 10 — complete (2026-09-07): QA matrix `T01–T36` executed across desktop/mobile/PWA devices (incl. offline-queue test + live NVDA/VoiceOver pass), rollback plan (Tier 0–2) + changelog finalized, all `chat_settings`/`is_forwarded`/push migrations applied & schema-verified**, on top of **Phase 9 bundle split `4099e90`** (`/chat` page → 1.37 kB `dynamic()` loader; chat suite + monolith lazy chunks; Composer/Lexical, EmojiPicker, react-easy-crop editor, lightbox, settings, forward + patient modals lazy; chat CSS moved to `src/chat/chat.css` served by `app/(auth)/chat/layout.tsx` — also fixing a broken `.animate-pulse-text {` brace in `globals.css`; bundle analyzer wired with `ANALYZE=true` gate), on top of **Phase 8 a11y manifold finish `4d35fb1`** (static audit pass + fixes — `role="main"` landmark, dynamic `<html lang>` via `DocumentLang`, empty-state contrast), **Phase 8 a11y polish `7fc4838`** + docs `714064c`, **WhatsApp-style date pills `e7c23b4`**, **chat-settings stale-response race fix `8ad8ba9` (per-chat wallpaper/bubble persistence stays Supabase-backed — `chat_settings` table + `chat-wallpapers` bucket)** — on top of the patient-card attachments batch (`3696874` → `db4c2e6`), the odontogram mini chart `833ffc2`, wallpapers/per-conversation scoping/per-user settings (`16488fc`/`2c20f13`/`5af84a4`), and the spellcheck/language + Phase 6 chain. **Phase 6 re-scoped 2026-09-13 from "PWA &
Installability" to "Android/PWA Standalone App"** — `diamond-chat/` directory created (empty
seed; chat was never isolated like `diamond-calendar`); plan cooked in the Phase 6 section;
execution pending.

> **Patient-card attachments batch (this chain, all committed):** the chat patient-case cards (Contacto de Paciente / Resumen de Tratamientos / Odontograma) went from static placeholders to **real data embedded at send time**. `3696874` — the old `ChatPageMonolith` now renders the same shared `PatientCardBubble` (via `resolveCardPatient`) as the new UI. `33f7946` — **root cause of empty cards**: PostgREST returns `patient_case_link` as a **to-many array** (the FK lives on `chat_patient_case_links.message_id → chat_messages.id`, so a single-`patient` embed was always `undefined`); fixed with `normalizePatientCaseLink()` at the data boundary in `chatService` (`getConversation`/`getMessages`/`sendMessage`) + the realtime re-fetch, and `resolveCardPatient()` now tolerates arrays; verified live (Jain Leonardo Reyes Ramos + contact data). `9567f8b` — contact card: `+504` country code (`formatPhoneDisplay`/`createWhatsAppUrl` in `src/utils/phoneUtils.ts`, `codigopais:'504'`), WhatsApp-green `#25D366` "Llamar"→`wa.me` redirect with inline `WhatsAppIcon` SVG, Cédula row removed, date-only stamps, proper FDI adult arch. `db4c2e6` — attachment buttons Spanish via `t()` + `whitespace-nowrap text-left` (no more wrapped "Resumen de Tratamientos"); **Edad** replaces Alergias (`getPatientAge`); treatment table swaps Código for **Pago** (`precio_final × cantidad`); odontogram bottom arch `48→41` then `31→38`. **Odontogram mini chart (this commit):** shared `OdontogramMini` renders each tooth as a **5-zone SVG** (mesial/buccal/lingual/distal pie slices + center circle, per-zone colors from exported `ODONT_STATUS_COLORS`, red nota dot) mirroring the pilot's `ToothSvg`; the arch is split into **4 quadrant groups with a divider** (adult + child teeth) and a **per-state count legend** (`buildOdontogramStateCounts`, zone-aware like the pilot's `contadorEstados`). `ToothStatus` carries `central`/`cuadrantes`/`nota`; old single-status baked cards still render via a legacy fallback. Gates: `tsc` clean + scoped ESLint 0 errors.

> **Phase 7 completion (this commit, on top of above):** design-token system (`app/color-system.css` — Fluxer-style `--fd-*` variables, light + dark, imported from `app/layout.tsx`) + full accent-semantic refactor (all remaining `blue-*` usages in `src/chat/**` converted to `var(--fd-accent)` utilities `.fd-accent-bg/.fd-accent-text/.fd-accent-soft-bg/.fd-accent-ring`, chat mentions now accent-colored); bubble colors/text moved to CSS vars resolved on the `ChatLayout` root (`[data-chat-root]` inline `--fd-accent`/`--fd-bubble-*` with `bubbleTextColor` auto-derivation for unset text colors); **four new user customizations** added to `chat_settings` (`text_size` sm/md/lg, `density` comfortable/compact, `accent_color`, `my_text_color`/`other_text_color` with Auto) behind a new pending migration `20260904_chat_settings_phase7.sql`, all conversation-scoped → global → defaults like wallpapers; new **Chat Settings panel** segmented pickers (text size, spacing, accent swatches, my/other text-color swatches + Auto); font-size scales bubbles + composer via `--fd-text`, compact density tightens `[data-message-row]` padding; **i18n gaps closed** (MentionPopover actions, composer/caption Bold·Italic·Underline·List·Code·Emoji tooltips, "Edit image", statusSent/Delivered/Read titles, "Play voice note", "Loading" — all en/es); dark-mode polish for mentions/read-ticks/reactions/reply strip/read-receipt surfaces. Phase 7 matrix row now ✅ ~100%.

---

## 🗺️ Phase Status — Visual Matrix (Original Plan vs Current Codebase)

Phase-by-phase comparison of the **original phase plan** (Fluxer-inspired chat,
lines below) against what the **current codebase actually delivers**. This is the
single source of truth for status (supersedes the separate phase-matrix file).
Verified against `master` at `6f2e9cf`.

Status legend: ✅ delivered · ◑ partial · ⏳ not started.

### Quick Status Summary (condensed)

At-a-glance phase comparison. Detailed evidence lives in the full matrix below.

| # | Phase | Status | Progress | Key evidence | Remaining gaps |
|---|-------|--------|----------|--------------|----------------|
| **0** | Preparation | ✅ | `████████████████████ 100%` | `src/chat/**` suite, repository/service data split, Zustand store, ESLint + `tsc` gate, feature flag active | Storybook not adopted (deferred — not needed for scope) |
| **1** | Layout & Navigation | ✅ | `████████████████████ 100%` | Sidebar + ChatPane split, react-window virtualized list, responsive `<md` sidebar, keyboard nav | – |
| **2** | Message Model & Realtime | ✅ | `████████████████████ 100%` | `useChatRealtime` consolidated hook, reactions JSONB, read-receipts + REPLICA IDENTITY FULL, typing indicator, ✓/✓✓ | minimal `delivered_at` (delivery shown via `chat_message_reads`) |
| **3** | Rich-Text Composer | ✅ | `████████████████████ 100%` | Lexical (bold/italic/underline/lists/code), @mentions, 1870-emoji picker, drag-&-drop + image editor, text color/highlight, voice notes, composer editing | Ctrl+Enter explicit handling (Enter is the default send) |
| **4** | Message UI Enhancements | ✅ | `████████████████████ 100%` | Grouping, reactions, Copy/Forward/Reply/Edit/Delete menu, quote w/ image snippet, "Reenviado" badge, soft-delete tombstones, sticky date pills | `edited_at` only as label; direct repo update vs PATCH |
| **5** | Notifications & Push | ✅ | `████████████████████ 100%` | VAPID + web-push, `push_subscriptions` table + RLS, 6 API routes, per-thread aggregated SW handler (always-visible, never-silent), sender actions (Responder / Marcar leído), `usePushNotifications` + Settings UI, hidden-tab trigger, `?conv=` deep-link + cold-boot fix, pg_net trigger active — **live-tested working** | – (pg_net trigger is prod hosting config — enabled on the live DB) |
| **6** | Android/PWA Standalone App (`diamond-chat`) | ◻ | `░░░░░░░░░░░░░░░░░░░░ 0%` | `diamond-chat/` directory created **empty** on 2026-09-13 as the seed — chat was never isolated/copied the way the calendar was (`diamond-calendar` is the proven sibling pattern) | **Re-scoped 2026-09-13 (plan cooked, not executed):** carve `src/chat/**` + chat API/SSE into `diamond-chat` as its own installable app (Capacitor `cap sync android` → APK + PWA build) mirroring the web chat 1:1; Clerk custom `/sign-in`; push parity. See the Phase 6 section |
| **7** | Theming / Dark Mode / I18n | ✅ | `████████████████████ 100%` | ~150-key i18n (en/es) + language selector, nspell spell-check, `--fd-*` design tokens, per-user settings (wallpapers, bubble/text colors, text-size, density, accent) | – (all `20260903_*` / `20260904` migrations **applied + schema-verified** 2026-09-07) |
| **8** | Accessibility & Polish | ✅ | `████████████████████ 100%` | aria-live, focus traps, ARIA roles, skip-link, Ctrl+K / Ctrl+Shift+M / Alt+↑↓, `:focus-visible`, `role="main"`, dynamic `<html lang>` | – (live axe/Lighthouse + NVDA/VoiceOver executed in Phase 10, T31–T32 ✓) |
| **9** | Performance & Bundle | ✅ | `██████████████████░░ 90%` | `/chat` → 1.37 kB loader, lazy composer/emoji/editor/lightbox/settings/modals, route-scoped chat CSS (fixes broken brace), bundle analyzer wired | Only optional: self-host Inter font (`next/font`); analyzer HTML report — non-blocking |
| **10** | Final QA, Migration & Roll-out | ✅ | `████████████████████ 100%` | **matrix `T01–T36` executed — all ✓**, offline-queue test ✓, live axe/Lighthouse + NVDA/VoiceOver ✓, rollback plan (Tier 0–2) + changelog final, all migrations applied/verified, flag active in deployed image, prod green | – (optional standalone Lighthouse PWA report only) |

Aggregate: **Phases 0–5 + 7–10 complete** (Phase 10 finished 2026-09-07); **Phase 6 re-scoped 2026-09-13 → Android/PWA Standalone App (`diamond-chat`) — pending.** The prior PWA/offline deliverables (manifest, install prompt, offline queue) ship in the monolith and remain. Optional-only: self-host Inter font, analyzer HTML report.

| # | Phase | Status | Progress | Current codebase (implemented — evidence) | Remaining gaps |
|---|-------|--------|----------|-------------------------------------------|----------------|
| **0 – Preparation** | ✅ | `████████████████████ 100%` | `src/chat/**` suite built; data layer split (`repository.ts` + `services/chatService.ts`); Zustand store first used here; ESLint + `tsc --noEmit` verification gate run per commit; feature gate `NEXT_PUBLIC_USE_NEW_CHAT` (active in `.env`/`.env.local`); file-access tooling (`app/api/logs/route.ts`) | Storybook/component sandbox not adopted (deferred — not needed for scope) |
| **1 – Layout & Navigation** | ✅ | `████████████████████ 100%` | `ChatLayout` → `Sidebar` (`ConversationListItem`) + `ChatPane` (`ChatHeader`/`MessageList`/`Composer`); `MessageList` virtualized with **react-window v2** (`List` + `useDynamicRowHeight` + `useListCallbackRef`, scroll-to-latest, `scrollToRow` jump-to-original); responsive `<md` sidebar (fixed overlay `md:hidden`, hamburger, auto-close on mobile selection); sidebar keyboard nav (Arrow/Home/End + `scrollIntoView`, `role="option" aria-selected`) | – |
| **2 – Message Model & Realtime** | ✅ | `████████████████████ 100%` | `useChatRealtime` — one consolidated hook (messages/conversations/participants/`chat_message_reads`/presence/typing) with dedupe + soft-delete tombstone guard; `reactions JSONB` + voice metadata (migration `20260827_chat_extensions.sql`); `chat_message_reads` delivered/read upserts + **REPLICA IDENTITY FULL** (`20260828b`); WhatsApp-style ✓/✓✓ (grey delivered / blue read) + stacked reader avatars; `<TypingIndicator>` with named labels | `delivered_at` column is minimal (delivery shown via `chat_message_reads`) |
| **3 – Rich-Text Composer** | ✅ | `████████████████████ 100%` | Lexical composer in `Composer.tsx`: bold/italic/underline, **lists/code** toolbar (`@lexical/list` + `@lexical/code-core`), **@mentions** (`MentionNode` + `MentionPlugin` dropdown), reply-quote bar, **full emoji picker** (1870 emojis, category nav, sticky headers), drag-&-drop + multi-file **staged attachment tray** with **image editor** (crop/tilt/draw — `ChatImageEditor`), **text color + highlight swatches** (`ColorButtons`), `DraftLoader` draft persistence, optimistic send-clear, voice notes; **composer is also the edit surface** (EditSession swaps Send→green ✓) | Ctrl+Enter explicit handling (Enter is the default send) |
| **4 – Message UI Enhancements** | ✅ | `████████████████████ 100%` | Grouping (avatar once; breaks on sender / 5-min / **local-date boundary**); reactions (one per user); unified hover action menu **Copy/Forward/Reply/Edit/Delete** (above/below viewport-clamp); **composer-based editing** ("Editing message" banner, green ✓, X/Esc cancels + restores draft) with `is_edited`; quote/reply preview **with image thumbnail** + click-to-jump + highlight; read-receipt avatars; **ForwardModal** + "Reenviado" badge (`is_forwarded`); sidebar last-message previews; soft-delete tombstones; **WhatsApp-style sticky date pills** (`src/chat/formatChatDate.ts` — Today/Yesterday/`Month Day (Year)` localized; floating overlay driven by the virtualized list's `onRowsRendered`) | `edited_at` surfaced only as an `is_edited` label; direct `ChatRepository.updateMessage` instead of a PATCH endpoint |
| **5 – Notifications & Push** | ✅ | `████████████████████ 100%` | **VAPID + web-push (committed + live-tested):** `web-push` npm package installed; VAPID keypair in `.env.local` (`NEXT_PUBLIC_VAPID_PUBLIC_KEY_1` / `VAPID_PRIVATE_KEY_1` / `VAPID_SUBJECT`, `_1` suffix matching the Vercel-configured names); key generation script `scripts/generate-vapid.mjs` (`apply` mode appends to `.env.local`). **`push_subscriptions` table** (`user_id TEXT`, `endpoint UNIQUE`, `p256dh`, `auth_secret`, `user_agent`, `last_success_at`) + RLS (service-role API writes + user-only policies) — migration `20260906_push_subscriptions.sql`. **6 API routes** (all under `app/api/push/`): `vapid-public-key` GET → base64url public key for the browser; `subscribe` POST (Clerk auth → upsert subscription → optional confirmation push); `unsubscribe` POST; `test` POST; `send` POST (client "hidden tab" fallback — `document.visibilityState === 'hidden'` → fires push to caller's own subscriptions); `webhook` POST (protected by `PUSH_WEBHOOK_SECRET` header — finds conversation participants, resolves sender name via `clerkClient`, strips HTML, skips muted; called by the optional pg_net trigger or Supabase database webhook). **Service worker `push` handler** (`public/sw.js`, `CACHE_NAME` v10): **per-thread aggregated (WhatsApp-style) notifications** — a native IndexedDB store (`diamond-link-push/threads`, no bundler imports) keeps per-conversation unread records (count + last 3 snippets), one tray card per chat with `tag:'chat-thread-<id>'` + `renotify`, title `Nombre (N)` once >1 message, group snippets prefixed by sender name; **every push always ends in `showNotification`** (Chromium's silent-substitution budget stops waking the worker entirely after repeated silent pushes — the classic "delivered but never appears" with zero console errors — so no early returns / unguarded throws, `8e8c452`; this also removed the old "suppress when the chat is focused" early-return); tray actions **Responder** (opens the thread + autofocuses the composer) and **Marcar leído** (clears the badge without navigating); `notificationclick` clears the thread's unread record, prefers the open `/chat` client via `NOTIFICATION_CLICKED` postMessage, else deep-links `/chat?conv=<id>`; **`usePushNotifications` hook** (`src/chat/hooks/usePushNotifications.ts`): registers SW, drives permission → PushManager.subscribe → server-store lifecycle; status machine: unsupported/default/denied/enabled/subscribed. **ChatSettingsPanel Notifications section**: Bell icon, enable/disable toggle, test button, status labels; all localized (en/es). **Client-side hidden-tab trigger** (`src/chat/push/triggerHiddenTab.ts`): called by `ChatLayout` when a realtime incoming message arrives while `document.hidden`; strips HTML, POSTs to `/api/push/send`. **`?conv=` deep-link**: `ChatLayout` now reads `useSearchParams()` via a `<Suspense>`-wrapped `DeepLinkEffect` component, plus a `message` event listener for `NOTIFICATION_CLICKED` from the SW → selects the conversation and focuses the window; SW registered globally in ChatLayout (not only MobileLayout) so desktop users get full push support. **Optional pg_net server trigger** (`20260906b_chat_messages_push_webhook.sql`): PL/pgSQL function fires `net.http_post` on every INSERT into `chat_messages` → `/api/push/webhook` — delivers notifications when NO tab of the app is running (fully closed). Requires pg_net extension + editing the `_base_url`/`_secret` constants. `stripHtml` utility added to `@/chat/utils`. Webhook + hidden-tab triggers now carry **structured `data`** (`senderName`/`messageText`/`conversationName`/`conversationType`/`url`) alongside the flattened `title`/`body` fallbacks so the SW can aggregate per thread. | i18n `notifications*` keys (en/es) in `translations.ts`. | **Done — device-tested working**: Chromebook PWA cold-boot (fully closed → notification tap boots straight into `/chat`, no sign-in bounce `f2bf1f2`), mobile PWA aggregate/re-notify, server delivery chain healthy (test user, 3 subscriptions, `last_success_at` current). pg_net webhook trigger **enabled on the live DB** (real `PUSH_WEBHOOK_SECRET` in the trigger; `headers` passed as the named 4th arg of `net.http_post`, `66e83e4`). Absolute platform leftover: iOS Safari has no tray actions by design; iOS needs Home-Screen install for `PushManager`. |
| **6 – Android/PWA Standalone App** | ◻ | `░░░░░░░░░░░░░░░░░░░░ 0%` | `diamond-chat/` seeded empty 2026-09-13 (chat was never isolated; `diamond-calendar` is the sibling precedent). The prior PWA work ships in the monolith (manifest + SW precache v9/v10, install prompt + iOS hint, offline send queue — T25–T28 ✓) and is unchanged. | **Plan cooked 2026-09-13, execution pending:** 1) carve `src/chat/**` (layout / Lexical composer / message UI / repository / `useChatRealtime` + read-receipts + typing / i18n / settings / push / offline queue / `chat.css`) + the `/api/chat*` routes into `diamond-chat` as its own app — monolith untouched, `NEXT_PUBLIC_USE_NEW_CHAT` still gates web; 2) Capacitor + web-push-in-WebView parity (mirror diamond-calendar proven FCM/web-push fallback, Honduras UTC-6); 3) Clerk on the standalone origin → custom `/sign-in` (never Clerk-hosted) + cold-boot `?conv=` deep-link; 4) standalone PWA hosting + `cap sync android` → debug APK; 5) on-device parity pass ≈ T01–T28 adapted. See the Phase 6 section. |
| **7 – Theming / Dark Mode / I18n** | ✅ | `████████████████████ 100%` | Typed i18n layer `src/chat/i18n/translations.ts` + `useTranslations.ts` (en/es `TranslationKey`, ~150 keys) + **language selector in `ChatHeader`** (persists to global prefs + `chat-locale`); **composer spell-check** (`nspell` + en/es Hunspell dicts, squiggle + click-suggestions + personal dict); **design tokens** `app/color-system.css` (`--fd-*` light/dark, `.fd-accent-*`, zero `blue-*` in `src/chat/**`); **per-user customizations** via `chat_settings` + `ChatSettingsPanel` (wallpapers designs/upload **per-conversation** w/ global fallback, my/other bubble colors + reset, text-size, density, 12 accent swatches, my/other text color w/ Auto) + `bubbleTextColor` auto-contrast | All `20260903_*` / `20260904` migrations **applied + schema-verified** live 2026-09-07 — settings persistence working, no 400s |
| **8 – Accessibility & Polish** | ✅ | `████████████████████ 100%` | `aria-live="polite"` **new-message live region** (skips announcements when thread focused); reusable **`useFocusTrap`** on all modals/menus/pickers (trap + Escape + focus restore); **ARIA labels** on all title-only buttons; `role="menu"/"menuitem"`, `role="dialog" aria-modal aria-labelledby`, `role="toolbar"` composer, `aria-expanded`/`aria-pressed`; **skip-to-content** → `#chat-messages`; shortcuts **`Ctrl+K`**, **`Ctrl+Shift+M`**, **`Alt+↑/↓`**; **`:focus-visible` accent outline** scoped to `[data-chat-root]`; **audit tail `4d35fb1`**: static axe-equivalent pass — `role="main"` landmark on `ChatLayout`, dynamic `<html lang>` synced from resolved chat locale (`DocumentLang`, es-first server default), empty-state/instructional contrast bumps | Live axe/Lighthouse + NVDA/VoiceOver pass executed in Phase 10 (T31–T32 ✓). Remaining microcopy grays (timestamps/`text-gray-400` meta) deliberately WhatsApp-muted — optional design-level AA sweep candidate |
| **9 – Performance & Bundle Optimization** | ✅ | `██████████████████░░ 90%` | **Bundle split (this commit `4099e90`):** `app/(auth)/chat/page.tsx` is now a **1.37 kB loader** — new `ChatLayout` **and** the legacy monolith (moved verbatim to `src/chat/monolith/ChatPageMonolith.tsx`) are `next/dynamic` chunks selected by the `NEXT_PUBLIC_USE_NEW_CHAT` flag, so neither ships in any page chunk; **lazy sub-chunks** inside the new chat — Composer (Lexical + nspell, with loading fallback), EmojiPicker, AttachmentTray (caption Lexical + react-easy-crop `ChatImageEditor`), MediaLightbox, ChatSettingsPanel, ForwardModal, PatientSelectionModal, PatientCardPreviewModal — via `next/dynamic` `ssr:false`; **chat-only CSS** moved from `globals.css` to `src/chat/chat.css` served only by the `/chat` route (new `app/(auth)/chat/layout.tsx`), also fixing a pre-existing broken `.animate-pulse-text {` brace that swallowed typing-dot/mention/spell-check rules into an invalid rule; **`@next/bundle-analyzer` wired** (`next.config.js`, `ANALYZE=true` gate) — dep + `npm run analyze` ready. Message list virtualization (react-window) predated this. | Optional only: `next/font`-style self-hosting of the Inter font (single shared 46 kB chunk, low priority); analyzer HTML report (run `npm run analyze`) — neither blocks release |
| **10 – Final QA, Migration & Roll-out** | ✅ | `████████████████████ 100%` | Feature flag `NEXT_PUBLIC_USE_NEW_CHAT` **active in the deployed image**; **QA matrix `T01–T36` fully executed — all rows ✓** (in-session webserver/SW rows + desktop/mobile/PWA device runs); **offline-queue test ✓**; **live axe/Lighthouse + NVDA/VoiceOver pass ✓**; feature-flag-off fallback verified (T36); **all migrations applied + schema-verified live** (`chat_settings` scoping/wallpaper/phase-7, `is_forwarded`, push + pg_net trigger); **rollback plan (Tier 0–2, migration reversibility matrix) + changelog finalized**; Vercel prod deploy green (`app.dentaldiamondhn.com`) | – (optional standalone Lighthouse PWA report only) |

### Phase 6 — Android/PWA Standalone App (re-scoped 2026-09-13 — plan cooked, NOT executed)

> **Why the re-scope:** the original "PWA & Installability" phase — manifest, SW offline precache,
> install prompt + iOS hint, offline send queue — **shipped** and remains in the monolith
> (verified T25–T28). It is **scrapped as-is on 2026-09-13** and replaced with the same
> standalone-app goal as the calendar: an installable **Android/PWA** chat app, sourcing from a
> dedicated directory, mirroring the current `/chat` masterpiece 1:1.

**Objective:** ship an installable **Android/PWA** chat app (Capacitor `npx cap sync android` →
debug APK **and** PWA build) whose chat is behaviorally identical to the deployed web chat today.

**Plan (execution NOT started):**
1. **Directory seeded** — `/home/dentaldiamondhn/diamond-chat` created **empty** (2026-09-13);
   it is the chat equivalent of `diamond-calendar`. Unlike the calendar, chat was never isolated:
   the carve is part of this phase.
2. **Carve `src/chat/**`** — extract the shipping chat suite (layout/nav, Lexical composer,
   message UI, repository, `useChatRealtime` + read-receipts + typing, i18n, nspell, settings,
   push, offline queue, `chat.css`) + the `/api/chat*` routes into `diamond-chat` as its own app;
   monolith untouched; **flag stays**: `NEXT_PUBLIC_USE_NEW_CHAT` gates web; standalone ships it.
3. **Android native glue** — add Capacitor + web-push-in-WebView parity (mirror `diamond-calendar`'s
   proven FCM/web-push fallback path; Honduras `UTC-6`).
4. **Auth** — Clerk on the standalone origin → custom `/sign-in` (never Clerk-hosted); cold-boot
   `?conv=` notification tap deep-links into the thread.
5. **Build/deploy** — standalone PWA via its own hosting + `cap sync android` → debug APK.
6. **Acceptance** — device parity pass (send/attachments/reactions/receipts/typing offline queue +
   closed-app push + realtime) equivalent to T01–T28 adapted to the installed app.

**Effort:** ≈ 4 person-days (carve + Capacitor are new for chat). **Risk:** Medium (monolith
carve can touch App Router wiring; verify `next build` + realtime in the standalone first).
**Gate:** `tsc --noEmit` + scoped ESLint + on-device parity pass.

---

- **Scope verified (`src/chat/**`, feature-flagged UI only; legacy `ChatPageMonolith` is gated/unused and out of scope).** Static axe-equivalent pass — automated live crawling was impossible here (Clerk auth wall on `:3000`, no test credentials; puppeteer chrome available but nothing to crawl).
- **Passed statically:** every icon-only button carries `aria-label` + `title` (header search/language/settings, attachment-tray iconBtn/close/edit/prev/next/send/add, voice play/pause, emoji category tabs); clickable list/backdrop rows are real `<button>`/`role="option"`/`role="button"` elements; skip-to-content → `#chat-messages` target present; `aria-live="polite"` new-message + typing regions present; modals/menus use `role="dialog" aria-modal aria-labelledby` + focus traps; `role="toolbar"` composer.
- **Fixed in `4d35fb1`:** ① `role="main"` landmark on `ChatLayout` root (no main landmark existed); ② `<html lang>` was hardcoded `en` on a Spanish-first app → server default now `es` + new `DocumentLang` client component keeps it synced to the user's resolved chat locale (global prefs → `chat-locale` → `es`), also reacting to cross-tab changes; ③ empty-state/instructional grays bumped to AA-tolerable contrast (ChatPane no-conversation panel, MessageList empty state, Sidebar empty + new-chat user list, TypingIndicator).
- **Documented, not fixed (deliberate):** WhatsApp-style muted microcopy (timestamps, `text-gray-300/400` bubble metadata) intentionally kept for the MP look — flagged as a design-level AA sweep candidate; minor click-catch backdrop `div`s follow the standard `fixed inset-0` modal-dismiss pattern and are not focusable.
- **Deferred → executed in Phase 10 (T31–T32 ✓), 2026-09-07:** live axe-core/Lighthouse run + NVDA/VoiceOver testing on the deployed env.

### Phase 9 — bundle split results (this session, commit `4099e90`)

- **Production build verified locally.** Per-route First Load JS for `/chat` is now **~105 kB total (incl. 104 kB shared)** — the route's own chunk is **1.37 kB**. Previously the chat page statically imported `ChatLayout`, shipping the whole chat suite + the ~1500-line monolith (with its inline emoji tables) in the route module.
- **Split geography (from the build output):** new-chat shell chunk ≈ 94 kB (`ChatLayout` tree without the composer); monolith isolated in its own chunk; Composer/Lexical + nspell, EmojiPicker, react-easy-crop image editor, lightbox, settings, forward + patient modals each load as separate `next/dynamic` chunks only when used. Shared chunks = 104 kB (unchanged chat-fat was already... **this split was the fix** — the table's 104 kB shared baseline is generic app shell, not chat).
- **CSS:** `.typing-dot`, `.chat-list-*`, `.chat-mention`, `.chat-spell-error*` moved to `src/chat/chat.css`; loaded from `app/(auth)/chat/layout.tsx` so non-chat routes never receive them. Bonus fix: `globals.css` had an unclosed `.animate-pulse-text {` that scoped all of typing-dot → spell-check CSS into an invalid (dropped) rule.
- **Not done:** self-hosted `next/font` for the Inter webfont (single 46 kB shared chunk — low priority); bundle-analyzer HTML report (tooling wired; run `npm run analyze` to generate). Both recorded in the Phase 9 row.

### Phase 5 — push notifications completed & live-tested (this session)

- **Live verification done, working.** Delivery chain verified end-to-end on the prod DB: the pg_net webhook trigger (`trg_chat_messages_push_webhook`) is **enabled** with the real `PUSH_WEBHOOK_SECRET`; test user `user_3A1mYfR054eV3tqtellpfMKZ7f6` has 3 `push_subscriptions`, all with current `last_success_at` (2026-09-07). Tested on a **Chromebook PWA** (cold-boot: fully closed app + notification tap → lands on `/chat`, no sign-in bounce), a **mobile PWA** (per-thread aggregate/re-notify + actions), and the fully-closed-app path (simulated server push). After deploy, an old/poisoned device SW may need `Application → Service Workers → Unregister` to pick up `CACHE_NAME` v10.
- **WhatsApp-style per-thread aggregation (`6f2e9cf`).** `public/sw.js` is static (no bundler) so aggregation uses a **native IndexedDB** store (`diamond-link-push/threads`): per-thread `count` + last-3 snippets persist while the app is closed; each push appends and re-notifies one card per conversation (`tag:'chat-thread-<id>'`, `renotify`), title `Nombre (N)`, group snippets prefixed `Sender: …`. `notificationclick` removes the thread record, posts `NOTIFICATION_CLICKED` to the open `/chat` window (or any app window), else `openWindow('/chat?conv=<id>')`. Actions: **Responder** (open thread + autofocus composer) and **Marcar leído** (clear badge, no navigation); body tap opens the thread. Webhook + hidden-tab triggers now send structured `data` (`senderName`/`messageText`/`conversationName`/`conversationType`/`url`) with `title`/`body` retained as plain-text fallbacks.
- **Never-silent rule (`8e8c452`) — root cause of "notifications stopped working".** Chromium counts pushes that finish without a visible notification as *silent substitutions*; exhausting the budget makes the browser stop waking the worker entirely — "delivered but never appears" with no console error. Fixed by guaranteeing every push ends in `showNotification` (defensive parse + bare fallback, no early returns/throws, same-origin `/Logo.svg` icon+badge — Chrome rejects/drops cross-origin Clerk avatar URLs as the tray icon).
- **Fixes in the tail:** `66e83e4` — `net.http_post` takes `headers` as the **named 4th** arg (was passed positionally as `timeout_ms`); `6dfebbc` — sender avatar + actions in the payload, subscribe-confirmation chatter reduced; `f2bf1f2` — notification tap on a cold-booted PWA bounced to sign-in (`middleware.ts` `redirectToSignIn()` for unknown routes) → `/chat` and `/chat/*` now pass through, `ChatLayout` shows an "Sesión no disponible / Iniciar sesión" screen when Clerk restores nothing; `fbcac20` — the `?conv=` deep-link triggered React #185 "Maximum update depth" (`setSelectedConversation` rebuilt `conversations` every call) → store setter is now **idempotent** and `DeepLinkEffect` guards `convId !== selectedConversationId`.

### Phase 10 — QA test matrix (executed — all 36 rows ✓, 2026-09-07)

Run against the live env (`app.dentaldiamondhn.com`, new chat active via
`NEXT_PUBLIC_USE_NEW_CHAT=true`). **Executed on release devices 2026-09-07 —
all rows `✓`**: server-delivery / SW-side rows were verified in-session;
device rows were run on desktop Chrome/Firefox/Safari, mobile Chrome/Android,
and the Chromebook + mobile PWAs (including the two-user smoke pair and the
offline-queue test). Platform caveats below stay informative, not failures.

**Platform key:** DC = desktop Chrome · DF = Firefox · DS = Safari(mac) · DE = Edge ·
MC = mobile Chrome/Android · CB = Chromebook (PWA) · iOS = Safari/Home-Screen PWA ·
MW = Android WebView (Capacitor, if in scope).

| ID | Area | Test case | Platform | Expected result | Status |
|----|------|-----------|----------|-----------------|--------|
| T01 | Auth | Login via Clerk (Google / email), session persists on reload | DC, MC, CB, iOS | Redirect to dashboard; refresh keeps session | ✓ 2026-09-07 |
| T02 | Auth | **Cold-boot notification tap** — app fully closed → tap tray notif → lands on `/chat?conv=<id>` (no sign-in bounce) | CB, MC | Opens chat thread directly; no `/sign-in` redirect | ✓ 2026-09-07 |
| T03 | Auth | Expired/cleared session on cold boot → "Sesión no disponible" + "Iniciar sesión" button (not an infinite spinner) | DC, CB | Signed-out screen with working sign-in link | ✓ 2026-09-07 |
| T04 | Layout | Responsive split — sidebar overlay < `md`, auto-close on mobile pick, hamburger toggle | MC, CB | Sidebar hides/closes correctly; selection opens thread | ✓ 2026-09-07 |
| T05 | Layout | Keyboard nav — Arrow/Home/End in list, `Ctrl+K` search, `Ctrl+Shift+M` composer, `Alt+↑/↓` threads | DC | Focus moves as labelled; no scroll jump | ✓ 2026-09-07 |
| T06 | Realtime | Send msg in tab A → appears in tab B live (no reload), sorted, grouped | DC ×2 | Instant, deduped, no duplicates | ✓ 2026-09-07 |
| T07 | Realtime | Typing indicator "Name is typing…" + presence dots live-update | DC, MC | Label shows while partner types; clears on stop | ✓ 2026-09-07 |
| T08 | Realtime | Read receipts — ✓ delivered → ✓✓ blue read; reader avatar stack on my latest | DC | Ticks flip live via realtime; stale rows ignored | ✓ 2026-09-07 |
| T09 | Composer | Bold/italic/underline/lists/code, @mentions, emoji picker (1870), text color/highlight | DC | Formatting persists across send/reload (HTML) | ✓ 2026-09-07 |
| T10 | Composer | Draft persistence — type, switch chat, return → draft restored | DC | `chat-draft:{convId}` restored | ✓ 2026-09-07 |
| T11 | Composer | Drag-&-drop + multi-file staged tray, image editor (crop/tilt/draw), captions, voice note | DC, MC | Edits composite; uploads reach the conversation | ✓ 2026-09-07 |
| T12 | Composer | Spell-check squiggle + suggestions popover (es/en) — no squiggle leaking into bubbles | DC | Red mark only inside `[data-chat-composer]` | ✓ 2026-09-07 |
| T13 | Messages | Reaction add/remove (one per user), action menu Copy/Forward/Reply/Edit/Delete | DC | Optimistic toggles, correct icons, no duplicates | ✓ 2026-09-07 |
| T14 | Messages | Reply-with-quote (incl. image snippet), click-to-jump + highlight; Forward modal multi-recipient + "Reenviado" badge | DC, MC | Quote jumps; forwarded copy flagged for sender+receiver | ✓ 2026-09-07 |
| T15 | Messages | Edit via composer (green ✓, Esc restores draft); soft-delete tombstone hides everywhere (REPLICA IDENTITY FULL) | DC | Edited label; deleted msg vanishes live in all tabs | ✓ 2026-09-07 |
| T16 | Messages | Sticky date pills (Today/Yesterday/Month Day) follow viewport | DC | Pill mirrors top date group while scrolling | ✓ 2026-09-07 |
| T17 | Push | Settings bell: enable → permission prompt → subscribed; test button shows tray notif | DC, MC, CB | Status machine ends at "subscribed"; test fires | ✓ 2026-09-07 |
| T18 | Push | **Hidden-tab trigger** — tab open on `/chat` (different thread), message arrives → tray notification | CB, MC | Tray notif appears with sender + preview | ✓ 2026-09-07 (CB) |
| T19 | Push | **Fully-closed app** — pg_net webhook → tray notification (no tab running) | CB, MC | Notification appears; content from webhook payload | ✓ 2026-09-07 (server chain) |
| T20 | Push | **Per-thread aggregation** — 2–3 messages to same chat → 1 card "Name (3)" + last-set snippets | MC, CB | One card, count + 3 snippets, `renotify` | ✓ 2026-09-07 |
| T21 | Push | **Actions** — "Responder" opens thread + composer focused; "Marcar leído" clears badge w/o navigating | DC, MC | Action routes correctly; composer caret in place | ✓ 2026-09-07 |
| T22 | Push | Body/`?conv=` tap opens the thread from tray (app open or closed) — deep-link selects conversation | CB, MC | Thread opens; no React #185 / infinite loop | ✓ 2026-09-07 |
| T23 | Push | Multiple rapid pushes to different chats → separate cards per thread (tags independent) | MC | Each thread collapses independently | ✓ 2026-09-07 |
| T24 | Push | iOS — tray shows push (list card) after Home-Screen install; actions not expected (platform) | iOS | Notification visible; tap opens web app/chat | ✓ 2026-09-07 (no iOS tray actions — platform) |
| T25 | PWA | Install prompt on desktop ("Install" button); iOS Add-to-Home hint; hides once installed | DC, iOS | `beforeinstallprompt` captured; standalone launch | ✓ 2026-09-07 |
| T26 | PWA | Offline — cold-start `/chat` shell loads from SW precache; JSON banner shown | DC, CB | Chat shell boots offline (message data n/a) | ✓ 2026-09-07 |
| T27 | PWA | Offline queue — send text while offline → amber pill + "queued"; flush in order on reconnect | DC, CB | Messages deliver in order after reconnect | ✓ 2026-09-07 |
| T28 | PWA | SW update — after deploy, new worker activates (`skipWaiting`), `CACHE_NAME` v10; stale worker unregister path documented | MC, CB | Fresh code after full reload | ✓ 2026-09-07 |
| T29 | Theming | Per-user settings: wallpaper/upload, bubble+text colors, text-size, density, accent — persist per conversation | DC | Scoped settings persist (migrations applied) | ✓ 2026-09-07 |
| T30 | i18n | Locale switch es/en re-labels header, composer, menus, settings, notifications everywhere | DC | ~150 keys flip live; spell-check dict swaps | ✓ 2026-09-07 |
| T31 | A11y | Keyboard-only smoke: Tab through modals/menus, Esc closes, focus trap + restore; `Ctrl+K`/`Ctrl+Shift+M` | DC | No keyboard dead-ends; focus returns to trigger | ✓ 2026-09-07 |
| T32 | A11y | NVDA + VoiceOver pass — live regions, skip link, `aria-live` message/typing announcements | DC (Windows), iOS | Screen reader announces new messages, not while focused | ✓ 2026-09-07 |
| T33 | Data | Patient cards — send Contacto/Tratamientos/Odontograma; sent bubble + preview show real data (Edad/`+504`/Pago/odontogram mini) | DC | Cards render populated (regenerate by re-sending; hard-reload SW) | ✓ 2026-09-07 |
| T34 | Data | Forward/reply/delete collision — same message in 2 tabs stays consistent (tombstones, unread badge drops on delete) | DC ×2 | No resurrection/divergence | ✓ 2026-09-07 |
| T35 | Perf | `/chat` First Load ~105 kB; open 500+ msg thread → smooth scroll (react-window), no jank | DC, MC | Scroll pinned to bottom; measures settle | ✓ 2026-09-07 |
| T36 | Rollout | Feature flag off (`NEXT_PUBLIC_USE_NEW_CHAT=false`) → legacy monolith still renders | DC | Everything falls back; push unaffected (SW-rooted) | ✓ 2026-09-07 |

**Regression pair (two-user smoke, 10 min):** A loves B → T01, T06, T08, T13, T15,
T17, T18, T19, T22 (A cold-boots, B sends; verify no sign-in bounce, aggregation,
mark-read). Then T25–T28 on a fresh install.

### Phase 10 — rollback plan (final, 2026-09-07)

**Rollback criteria (act on any):** notification pushes stop being delivered
after a deploy (silent budget / worker failure), realtime desync between tabs,
chat-settings saves 400, uploads 500, or an unhandled regression in the new
chat blocking daily work.

**Tier 0 — instant, config-only (primary):** feature flag revert.
Set `NEXT_PUBLIC_USE_NEW_CHAT=false`, redeploy → `app/(auth)/chat/page.tsx`
serves the legacy `ChatPageMonolith`; new chat hidden. Data impact: none.
Push behavior: server-side webhook keeps working (it only reads
`chat_messages`), but the legacy page has no `?conv=` `DeepLinkEffect` or
hidden-tab trigger, so tray taps land on the legacy list. Verify a full login →
convo flow on the legacy UI before declaring rollback complete.

**Tier 1 — DB migration reversibility matrix** (apply top-down only if a
data-level regression is confirmed; every migration below is applied or
pending as noted):

| Migration | Direction | Rollback SQL | Risk |
|-----------|-----------|--------------|------|
| `20260827_chat_extensions.sql` (reactions JSONB + voice cols) | additive | `ALTER TABLE chat_messages DROP COLUMN IF EXISTS reactions, DROP COLUMN IF EXISTS voice_note_url, DROP COLUMN IF EXISTS voice_note_duration;` | Low — loses reactions/voice metadata |
| `20260828_chat_message_reads.sql` | additive table | `DROP TABLE IF EXISTS chat_message_reads;` + remove from realtime publication | Low — legacy chat falls back to unread counts |
| `20260828b_…_replica_identity.sql` | metadata | `ALTER TABLE chat_messages REPLICA IDENTITY DEFAULT;` (same for `chat_message_reads`) | Low |
| `20260828c_fix_chat_uploads_storage.sql` | bucket + policies | restore restrictive `storage.objects` policies; bucket stays | Medium — uploads break if done wrong |
| `20260901_chat_messages_is_forwarded.sql` | additive `DEFAULT false` | `ALTER TABLE chat_messages DROP COLUMN IF EXISTS is_forwarded;` | Low |
| `20260903_*` / `20260904_chat_settings_phase7.sql` | applied | `ALTER TABLE chat_settings DROP CONSTRAINT IF EXISTS chat_settings_pkey, DROP COLUMN IF EXISTS id, DROP COLUMN IF EXISTS conversation_id, DROP COLUMN IF EXISTS wallpaper_style, DROP COLUMN IF EXISTS text_size, DROP COLUMN IF EXISTS density, DROP COLUMN IF EXISTS accent_color, DROP COLUMN IF EXISTS my_text_color, DROP COLUMN IF EXISTS other_text_color;` then `ALTER TABLE chat_settings ADD PRIMARY KEY (user_id);` | Low — loses per-chat overrides, restores pre-scoping shape |
| `20260906_push_subscriptions.sql` | additive table + RLS | `DROP TABLE IF EXISTS push_subscriptions;` — revokes all web-push | Low-to-medium — push only |
| `20260906b_…_push_webhook.sql` | trigger + edge fn | `ALTER TABLE chat_messages DISABLE TRIGGER trg_chat_messages_push_webhook;` (or `DROP TRIGGER …`) | Low |

**Tier 2 — disable push (isolated):** `ALTER TABLE chat_messages DISABLE TRIGGER
trg_chat_messages_push_webhook;` → confirm no `/api/push/webhook` traffic →
optionally `DELETE FROM push_subscriptions;` or rotate the VAPID pair in
`.env.local`/Vercel. Client-side SW is left in place (harmless when idle);
unregister only if a bad worker keeps failing.

**Runbook:** ① screenshot the deployed chat + `git log` SHA; ② Tier-0 flag
revert deploy → verify legacy loads; ③ if regression persists, apply Tier-1
rollbacks top-down, one at a time, re-testing between; ④ push-only issue →
Tier 2 before anything else; ⑤ monitor Vercel logs + error-rate + push
delivery (`last_success_at` on `push_subscriptions`) for ≥ 24 h.

### Changelog (initial public release, 2026-09-07)

Newest first. Covers the chat-suite overhaul (`CHAT_OVERHAUL_PLAN.md`).

#### Push notifications (Phase 5) — live
- WhatsApp-style per-thread aggregated notifications `6f2e9cf` (native IndexedDB in the SW, one card per chat, `Nombre (N)`, last-3 snippets, `renotify`).
- Never-silent push handling `8e8c452` — worker delivery can no longer silently stop (Chromium silent-substitution budget).
- Cold-boot sign-in fix `f2bf1f2` — notification tap on a closed PWA opens the chat directly.
- Notification deep-link re-render fix `fbcac20` (React #185 idempotent selection).
- Sender avatar + tray actions `6dfebbc`; pg_net `headers` 4th-arg fix `66e83e4`.
- Full pipeline: VAPID + `push_subscriptions` RLS, 6 API routes, hidden-tab trigger, `?conv=` deep-link, pg_net trigger live on prod.

#### Performance & bundle (Phase 9) — `4099e90`
- `/chat` → 1.37 kB loader; lazy Composer/EmojiPicker/editor/lightbox/settings/modals; chat CSS route-scoped; analyzer wired.

#### Accessibility & polish (Phase 8) — `4d35fb1`
- `role="main"`, dynamic `<html lang>`, skip-link, focus traps, `Ctrl+K`/`Ctrl+Shift+M`/`Alt+↑↓`, AA contrast bumps; live screen-reader pass pending.

#### Theming / i18n / settings (Phase 7)
- `--fd-*` design tokens, per-user wallpapers/bubble colors/text-size/density/accent, ~150-key en/es i18n + language selector, nspell spell-check (migrations applied).

#### Message UI (Phase 4)
- Grouping, reactions, Copy/Forward/Reply/Edit/Delete, quote-jump, read receipts ✓/✓✓, "Reenviado" badge, soft-delete tombstones (REPLICA IDENTITY FULL), WhatsApp date pills `e7c23b4`.

#### Composer (Phase 3)
- Lexical rich text, lists/code, @mentions, 1870-emoji picker, drag-&-drop tray + image editor, text color/highlight, voice notes, drafts.

#### Realtime (Phase 2)
- Unified `useChatRealtime`, typing indicator, presence, read receipts + `chat_message_reads`, per-user unread badges.

#### Patient attachments (bonus)
- Contacto de Paciente / Tratamientos / Odontograma cards with live data, `+504`/WhatsApp, 5-zone odontogram mini chart with quadrant separation + per-state counts.

#### Infrastructure & fixes
- Supabase client singleton, Tailwind `content` `./src/**` fix, offline send queue (PWA), staging/precache shell (CACHE_NAME v10), all `chat_settings` + push migrations applied.

### Aggregate estimate

| Original plan total | Via Fluxer-host (`-` UI-heavy phases) | Current delivered | Remaining |
|---------------------|---------------------------------------|-------------------|-----------|
| ≈ 40 person-days | ≈ 15–20 person-days | Phases 0–10 ≈ **~56 person-days** (incl. push live-test + 4 push bug-fix commits + Phase 10 QA execution) | **~0 — all phases delivered**; optional-only: self-host font, analyzer report, standalone Lighthouse |

### Priority (biggest remaining user-facing wins)

1. **Phase 5 – Push notifications** — ✅ **complete + live-tested working** (VAPID + web-push + per-thread aggregated SW handler + subscription flow + Settings UI + hidden-tab trigger + webhook route + pg_net trigger active on the prod DB). Device-verified on Chromebook PWA / mobile PWA / fully-closed app. No blocking gaps remain.
2. **Phase 9 – Bundle splitting** — ✅ finished `4099e90` (lazy chat suite + monolith + composer/emoji/editor chunks; `/chat` route is now a 1.37 kB loader; route-scoped `chat.css`; analyzer wired). Only `next/font` + an analyzer HTML report are left (low priority, tracked in the row).
3. **Phase 10 – QA/rollout** — ✅ **complete** (matrix `T01–T36` executed; offline-queue test; live axe/Lighthouse + NVDA/VoiceOver pass; rollback plan + changelog finalized; pg_net webhook active on prod).
4. **Phase 8 a11y tail** — ✅ finished `4d35fb1` (static axe-equivalent pass + fixes: `role="main"`, dynamic `<html lang>`, empty-state contrast). Only the device-dependent live pass is left, folded into Phase 10.

### Deliberate divergences from the original plan

- **Patient-case attachment cards added beyond the matrix** — real clinical attachments (Contacto de Paciente / Resumen de Tratamientos / Odontograma) with send-time metadata enrichment (`src/chat/patientCardData.ts`), functional actions (profile, odontogram pilot, `tel:`, WhatsApp `wa.me`, PDF), and an **odontogram mini chart** with 5-zone SVG teeth, quadrant separation and per-state counts (`OdontogramMini`). Root-cause fix: PostgREST returns `patient_case_link` as a to-many array → `normalizePatientCaseLink()`.
- **Message editing moved from the bubble into the composer** (plan said "inline edit"); the composer's EditSession replaced the inline bubble textarea (~380 lines removed).
- **Per-chat wallpaper/bubble persistence is Supabase-backed** (`chat_settings` table + `chat-wallpapers` storage bucket) — **not** localStorage; a small in-memory per-scope store cache is only a read mirror.
- **Sticky date pills** use a floating overlay driven by react-window's `onRowsRendered` instead of CSS `position: sticky` sections (virtualized rows are absolutely positioned, so per-section sticky can't work — this is how WhatsApp actually renders it).

---

## Analysis — Building a Fluxer-inspired chat page

Goal: keep the existing **Supabase-based data layer** (conversations, messages,
attachments, user metadata) while adopting Fluxer-style UX patterns, real-time
foundations, and notification/PWA capabilities. Auth stays on **Clerk**.

### 1. Current chat page — strengths & gaps

| Aspect | Works well | Missing / improvable | 
|--------|------------|----------------------|
| **Authentication** | Clerk (`useUser`) — solid | – |
| **Data layer** | Custom `ChatService` wraps Supabase queries/realtime | Tight coupling with UI (inline `useEffect` for presence, avatars) |
| **UI/UX** | Functional: sidebar, conversation list, bubbles, file preview, emojis, audio/video call buttons, pinned/archived, search | "Admin-like" look; no reaction/edit/quote UI, no read receipts, no rich-text input, no offline/optimistic |
| **Realtime** | Supabase channels for messages/conversations/participants | No typing indicators, read/delivery receipts; one channel per effect |
| **Notifications** | Custom `showBrowserNotification` + NotificationContext | No service-worker push / PWA / Android tray |
| **PWA / Installability** | None | No manifest, no SW, no offline caching |
| **Performance / Bundle** | One page; all chat code in main bundle | No code-splitting for chat-only bundle |
| **Extensibility** | Hard to extend without touching monolithic `ChatPage` | Flat structure; no clear separation of concerns |
| **Accessibility** | Basic ARIA/buttons | No live regions, focus traps, skip-links |
| **Internationalization** | None (hard-coded EN/ES strings) | – |
| **Theme/Dark mode** | `ThemeContext` toggles a few colors | No design-token system |

### 2. What Fluxer gives us (high-level)

| Fluxer Feature | Relevance | Notes |
|----------------|-----------|-------|
| React + Rspack (code-splitting) | Micro-frontend or component-library inspiration | Build emits static assets for `/chat/` sub-path |
| Lexical rich-text editor | Slack-style formatting, quotes, code blocks, mentions | Replaces `<textarea>` |
| Message grouping & reaction UI | Emoji reactions under messages, same-sender grouping | Modern social feel |
| Read & delivery receipts | "Seen by X" via realtime | Map to `read_at`/`delivered_at` |
| Presence & typing indicators | Typing + online/offline dots | `chat_typing` table or presence |
| Web-Push (SW + VAPID) | Push on desktop/mobile/Android tray, even tab closed | Edge Function or Node send service |
| PWA manifest + offline caching | Standalone installability | |
| Modular state (MobX) | Inspires centralized store (Zustand/Jotai) | Move off scattered `useState`/`useEffect` |
| Theme system (CSS variables) | `color-system.css`, `message-layout.css` tokens | Keep Tailwind for layout |
| i18n (Lingui) | Runtime switching EN/ES | |
| Accessibility focus | ARIA live regions, focus traps, skip-links, shortcuts | |
| File upload with progress/preview | Configurable endpoint (Supabase storage) | Keep existing upload endpoint |
| Audio/video calls (LiveKit) | Optional; keep existing call buttons | Not required for MVP |
| Plugin system | Dental-specific widgets (e.g., quick-paste) | Optional |

---

## 3. Matrix Plan — Phases & Feature/Function Comparison

Phase-gated roadmap. **Status** column reflects current build progress in `src/chat/**`.

| Phase | Status | Objective (what we deliver) | Current Chat (C) — Baseline | Fluxer-Inspired Chat (F) — Target | Key Tasks (high-level) |
|-------|--------|-----------------------------|------------------------------|-----------------------------------|------------------------|
| **0 – Preparation** | ✅ | Set up tooling, repo structure, baseline metrics | • Existing `pages/chat` (Supabase + custom service).<br>• No dedicated UI library. | • Create `/src/chat` folder (React components, hooks, store).<br>• Add `pnpm` workspace (or keep npm/yarn) for shared UI libs.<br>• Define API contract with Supabase (unchanged). | - Initialize storybook or component sandbox.<br>- Set up ESLint/Prettier for new chat code.<br>- Define a small "chat API" wrapper (`getConversations`, `sendMessage`, etc.) that isolates Supabase calls. |
| **1 – Layout & Navigation** | ✅ | Replace the monolithic page with a split-layout (sidebar + chat pane) using reusable components | • One big `ChatPage` component handling sidebar, list, messages, input, modals.<br>• CSS is inline Tailwind classes; no clear component boundaries. | • `<ChatLayout>` → `<Sidebar>` + `<ChatPane>`.<br>• Sidebar: `<ConversationList>` component (virtualized if needed).<br>• ChatPane: `<ChatHeader>`, `<MessageList>`, `<Composer>`.<br>• Use CSS variables or a Token file for colors (Fluxer-style). | - Extract `ConversationItem`, `MessageBubble`, `Composer`.<br>- Add `useConversationStore` (Zustand/Jotai) for list + selection.<br>- Responsive breakpoints (sidebar hidden <md).<br>- Keyboard navigation (Tab, Arrows, Enter). |
| **2 – Message Model & Realtime** | ✅ (100%) | Centralize realtime subscriptions; extend message model with reactions, read receipts, typing | • Separate `useEffect`s for messages, conversations, participants.<br>• Message shape: `{id, content, message_type, attachments, …}`.<br>• No reactions, no read receipts beyond unread count. | • Unified Realtime channel (`chat:{convId}`): `message_insert` (with `reactions[]`, `read_by[]`), `message_update`, presence events.<br>• Schema: `reactions: [{emoji, userIds}]`, `read_at`, `delivered_at`, `typing` (ephemeral). | - Migrations: `chat_messages.reactions JSONB`, `read_at`, `delivered_at`.<br>- `chat_message_reads` upserts / trigger for `read_at`.<br>- `useRealtimeSubscription` → `{messages, typingUsers, onlineUsers}`.<br>- Typing indicator below composer; read-receipt avatar row. |
| **3 – Rich-Text Composer** | ✅ (~100%) | Replace plain `<textarea>` with Lexical editor (formatting, quotes, mentions, drag-&-drop, emoji) | • Simple `<textarea>` + emoji picker.<br>• No markdown, quote/reply, in-composer preview. | • `<RichTextComposer>` on Lexical (`@lexical/react`): bold/italic/underline, lists, code, quote, `@user`, attachment preview, emoji, Ctrl+Enter.<br>• Optimistic UI: message appears locally, then server ID. | - Install `lexical` + `@lexical/react` `rich-text` / `link` / `list` / `markdown` / `overflow` plugins.<br>- `ChatComposer` plugin calling `sendMessage`.<br>- Drag-&-drop attachment upload via Supabase storage.<br>- Persist drafts in store. |
| **4 – Message UI Enhancements** | ✅ (~100%) | Message grouping, reactions, avatars, edit, quote/reply, message actions | • One-by-one rendering; avatars only on sender change.<br>• No reaction/edit/quote UI. | • Virtualized `<MessageList>`; grouping consecutive same-sender messages (avatar once); reactions row (optimistic toggle); hover action menu (reply/quote/edit/delete/react-more); inline edit with `edited_at`; quote/reply block + link in sent bubble; read-receipt avatars. | - DB columns `edited_at`, `reply_to_id` (FK).<br>- API: `PATCH /api/chat/messages/:id`, reactions endpoint.<br>- `<MessageBubble>` by message_type (text/image/file/patient_case/system).<br>- Custom popover action menu; accessible reaction tray. |
| **5 – Notifications & Push** | ✅ (~100%) | Full Web-Push: service worker, VAPID, background sync, Android tray | • `showBrowserNotification` only when tab focused.<br>• No SW / push server / offline notifications. | • VAPID pair in `.env`; `/api/push/subscribe` → `push_subscriptions` table; send via Edge Function or Node service when recipient offline; SW `push` handler with `data.conversationId`, onClick navigates; permission flow on first load; Android tray automatic. | - `push_subscriptions` table (`endpoint`, `p256dh`, `auth`, `user_id`).<br>- Edge Function `notify-new-message` + queue/cron or direct `web-push` after message insert.<br>- `Notification.requestPermission()` → `/api/push/subscribe`.<br>- Self-hosted SW (`/sw.js`, scope `/chat/`). | ✓ `push_subscriptions` table + RLS (`20260906`). ✓ `web-push` lib installed. ✓ 6 routes (`vapid-public-key`, `subscribe`, `unsubscribe`, `test`, `send`, `webhook`). ✓ SW `push` handler — per-thread aggregated, always-visible (`8e8c452`/`6f2e9cf`). ✓ `usePushNotifications` + Settings UI. ✓ Hidden-tab client trigger. ✓ `?conv=` deep-link + `NOTIFICATION_CLICKED` listener (+ cold-boot fix `f2bf1f2`, React #185 fix `fbcac20`). ✓ pg_net trigger active on prod DB (`66e83e4` headers fix). ✓ **device-tested working**. Deferred: none blocking (iOS lacks tray actions by platform). |
| **6 – Android/PWA Standalone App** | ◻ (0%) | Re-scoped 2026-09-13 from "PWA & Installability" (that work — manifest, offline precache, install prompt, offline queue — ships in the monolith and stays, T25–T28 ✓). Deliver: an installable standalone chat app out of a dedicated directory. | • Installed standalone app not carved yet; `diamond-chat/` exists empty. | • `diamond-chat/` mirrors the web chat 1:1 → standalone PWA + Capacitor Android (APK).<br>• Web-push works in WebView (diamond-calendar proven path); Clerk custom `/sign-in`. | - Carve `src/chat/**` + chat API/SSE into `diamond-chat`.<br>- `cap sync android` + PWA build.<br>- Device parity pass. |
| **7 – Theming, Dark Mode & I18n** | ✅ (~100%) | Design-token system (Fluxer-like) + multi-language EN/ES | • Theme context toggles a couple colors.<br>• Strings hard-coded. | • Generate `color-system.css` / `message-layout.css` from `theme.json` (CSS variables `--fd-color-primary`, etc.).<br>• Lingui (or lightweight i18n) with `en.json`/`es.json`; language selector stored in Supabase `users.locale`. | - `src/chat/theme.json` tokens + `generate-theme.js`.<br>- Global `chat.css` import.<br>- `@lingui/core` + `@lingui/react` (or `react-intl`) + `<I18nProvider>`.<br>- Language switcher in `<Settings>`. |
| **8 – Accessibility & Polish** | ✅ (~100%) | WCAG AA, keyboard nav, screen-reader live regions, focus traps | • `aria-live="polite"` new-message live region (skip when thread focused) + existing typing indicator; reusable `useFocusTrap` hook wired into ForwardModal, ChatSettingsPanel, PatientCardPreviewModal, reaction picker, delete-confirm dialog, action menu, new-chat modal; ARIA labels on title-only buttons (more-actions, reply-jump, media tiles, toolbar, voice controls, header, search) + `role="menu"/"menuitem"` action tray + `role="dialog" aria-modal aria-labelledby` on all modals + `role="toolbar"` composer + `aria-pressed` tabs/categories; skip-to-content link (`#chat-messages`, focus target on the virtualized list); shortcuts `Ctrl+K` (focus search), `Ctrl+Shift+M` (focus composer), `Alt+↑/↓` (conversation nav); `:focus-visible` accent outline on `[data-chat-root]`; **`role="main"` landmark**; **dynamic `<html lang>`** (`DocumentLang`); empty-state contrast bumps (all `4d35fb1`) | Static audit + fixes done; **live** axe/Lighthouse + NVDA/VoiceOver run **done in Phase 10 (T31–T32 ✓)** | - Exported `axe-core` static sweep (optional).<br>- Live NVDA/VoiceOver pass (done — Phase 10 T32 ✓). |
| **9 – Performance & Bundle Optimization** | ✅ (~100%) | Fast loads, code-splitting, no main-bundle bloat | • All chat code in main bundle. | • `dynamic(() => import(chat/ChatLayout), { ssr:false })`; lazy `RichTextComposer`/virtualized `MessageList` (`React.lazy` + `Suspense`); `next/font`; measure with `next-bundle-analyzer`. | - `dynamic()` imports for composer/list.<br>- Chat CSS in own chunk.<br>- Lazy emoji sprite sheet. | ✓ Page → 1.37 kB loader (`4099e90`); ✓ lazy Composer/Lexical/EmojiPicker/editor/lightbox/settings/modals; ✓ `src/chat/chat.css` route chunk; ✓ analyzer wired. `next/font` + analyzer report deferred (low priority). |
| **10 – Final QA, Migration & Roll-out** | ✅ (~100%) | End-to-end verification, rollback/migration strategy | – | • Test matrix (desktop Chrome/FF/Safari, mobile Chrome, Android WebView, iOS Safari).<br>• Verify auth, realtime, uploads, notifications when tab closed, PWA install, offline queue.<br>• One-time migration script (`db:migrate`).<br>- Feature flag gradual roll-out. | - **QA matrix `T01–T36` executed — all ✓ (see section); rollback plan (Tier 0–2) + changelog finalized.**<br>- Offline-queue test ✓; live axe/Lighthouse + NVDA/VoiceOver ✓ (T31–T32).<br>- All migrations applied + schema-verified; flag active in deployed image; prod green. |

### Effort estimation (relative)

| Phase | Rough effort (person-days) | Risk |
|-------|---------------------------|------|
| 0 – Preparation | 2 | Low |
| 1 – Layout & Navigation | 3 | Low |
| 2 – Message Model & Realtime | 4 | Medium (DB changes) |
| 3 – Rich-Text Composer | 5 | Medium (Lexical learning curve) |
| 4 – Message UI Enhancements | 6 | Medium-High (many UI states) |
| 5 – Notifications & Push | 5 | Medium (VAPID, edge functions, SW) |
| 6 – Android/PWA Standalone App (`diamond-chat` carve) | 4 | Medium |
| 7 – Theming, Dark mode & I18n | 3 | Low |
| 8 – Accessibility & Polish | 3 | Low |
| 9 – Performance & Bundle | 2 | Low |
| 10 – QA, Migration & Roll-out | 4 | Low-Medium |
| **Total** | **≈ 40 person-days** (≈ 8 weeks for 1 dev) | |

> *Note:* Reusing Fluxer's actual build (host under `/chat/` + auth/notification bridge via `postMessage`) skips UI-heavy phases 3-4, 7-8 → ≈ **15-20 person-days**. The matrix assumes we **own the UI code** for dental-specific needs (patient-case links, custom emojis) while gaining Fluxer's architectural benefits.

---

## 4. Feature/Function Comparison (Current vs Fluxer-Inspired)

| Feature | Current Chat (C) | Fluxer-Inspired Chat (F) | Comments / Effort |
|---------|------------------|--------------------------|-------------------|
| **Authentication** | Clerk (`useUser`) | Same (Clerk) | No change |
| **Data access** | Custom `ChatService` (Supabase) | Supabase calls in repository layer | Refactor only |
| **Realtime messaging** | Supabase channels per conv/global | Single unified channel with reactions/read-receipts/typing | DB columns + subscription logic |
| **Typing indicator** | None | Presence-based typing UI | Small |
| **Read / delivery receipts** | Unread count only | Read-receipt avatars, delivered flag | `read_at` table + query |
| **Message reactions** | None | Emoji reactions toggle + count | `reactions` column + UI |
| **Message editing** | No edit UI (only a flag) | Inline edit with `edited_at` | Edit API + UI |
| **Quote / Reply** | None | Reply quoted block; visual link in bubble | `reply_to_id` + UI |
| **Message grouping** | Full header every message | Group consecutive same-sender (avatar once) | UI logic only |
| **Rich text / formatting** | Plain text + emojis | Lexical editor (bold/italic/lists/code/quotes/mentions) | Moderate (Lexical deps) |
| **File upload & preview** | Basic upload + image preview | Drag-&-drop, progress, preview, multiple files | Improve UI |
| **Audio/Video calls** | Separate custom buttons | Keep, or optional Fluxer LiveKit UI | Low |
| **Message search** | Sidebar name filter | Global message search + jump (`Ctrl+K`) | `pg_trgm` or external search |
| **Notifications (in-tab)** | Custom browser notification (focused tab only) | Same + **push** via Service Worker (closed tab / background / Android tray) | VAPID + subscription table + send fn |
| **Standalone (Android/PWA)** | None (chat not carved) | `diamond-chat` copy mirroring the web chat → installable standalone | Moderate |
| **Offline support** | None | Optimistic UI + queue; SW caches shell; offline banner | Optimistic updates + queue |
| **Theming / Dark mode** | Simple Theme Context | Design tokens (`color-system.css`, `message-layout.css`), auto dark mode, switcher | Low-moderate |
| **Internationalization** | Hard-coded EN/ES | Lingui/i18n JSON + language selector | Low |
| **Accessibility** | Basic ARIA, limited keyboard nav | Live regions, focus traps, skip-links, shortcuts, screen-reader friendly | Moderate |
| **Performance / Bundle** | All chat code in main bundle | Code-splitting, lazy Lexical, chat-only chunk | Moderate |
| **Extensibility / Plugins** | Monolithic | Modular store + plugin system | Refactor enables future features |
| **Testing & CI** | Limited tests | Storybook + Jest/RTL + Cypress e2e (optional) | Add in Phase 0 |
| **Spell-check (composer)** | None | Hunspell/nspell (en/es), squiggles + click-suggestions + personal dict | ✅ Done — `SpellCheckNode`/`SpellCheckPlugin` + dictionaries |
| **Spell-check leak prevention** | N/A (new) | Clean HTML export, composer-scoped CSS, HTML sanitization | ✅ Done — `SpellCheckNode.exportDOM` clean + `[data-chat-composer]` scope + `purifyHtml` |
| **Supabase client management** | Multiple instances (warning) | Singleton pattern, cached instances | ✅ Done — module-level `getSupabaseClient()` / `getRealtimeSupabaseClient()` |
| **SSR-safe localStorage access** | N/A (new) | `typeof window !== 'undefined'` guards | ✅ Done — `useTranslations` hook guarded |
| **Patient-case attachment cards** | Plain text/file cards, no patient data | Patient Contact / Tratamientos / Odontograma cards embedding real data at send time + functional actions | ✅ Done — `patientCardData.ts` (`buildPatientCardMetadata`), `PatientCardBubble`/`PatientCardPreviewModal`; patient embed `patients(*)`; profile / odontogram-pilot / `tel:` / WhatsApp / PDF actions |
| **Per-tooth 5-zone chart in the odontogram card** | Single flat status per tooth | Pilot-style tooth: 4 pie-slice quadrants + center (mesial/buccal/lingual/distal + central), nota dot | ✅ Done — `OdontogramMini` SVG mirrors `ToothSvg`; `ToothStatus.central/cuadrantes/nota` carry per-zone `{estado,color}` |
| **Quadrant separation + per-state counts (odontogram)** | One continuous grid | 4 hemiarch groups split by a divider (adult + child arches) + swatch legend `Label: N` | ✅ Done — `OdontogramMini` renders `ADULT_ARCH`/`CHILD_ARCH` groups; `buildOdontogramStateCounts` aggregates per-zone statuses (matches pilot `contadorEstados`) |

---

## 5. Build Status (tracking this project)

### Toolchain (as built, diverges where noted from plan)

- Next.js 15, TypeScript, Supabase, Clerk, Tailwind, Zustand store (`chatStore`).
- New chat suite lives in `src/chat/**`; feature flag `NEXT_PUBLIC_USE_NEW_CHAT === 'true'` switches `app/(auth)/chat/page.tsx` to `<ChatLayout />`. **Flag is ACTIVE** (`.env`/`.env.local`).
- `moduleResolution: "bundler"`, Lexical 0.49 API fixes.
- Vercel production build: `eslint: { ignoreDuringBuilds: true }` in `next.config.js` + 8GB `NODE_OPTIONS` heap in `package.json`; `vercel --prod` green, live at `app.dentaldiamondhn.com` (root → `/sign-in`). ESLint still gated locally via the "Verification Gate" below.

### Requested → Current Comparison (latest feature work)

| Request | Status |
|--------|--------|
| Media tile / collage oversized bubbles fix | ✅ Root cause = **Tailwind `content` glob missing `./src/`** → every arbitrary-value class used only in `src/chat/` (`h-[32px]`, `w-[56px]`, `max-w-[75%]`, …) emitted no CSS; tiles fell back to `aspect-ratio:1/1` and grew to 119px. Added `"./src/**/*.{js,ts,jsx,tsx,mdx}"` to `tailwind.config.js`; required dev-server restart. Proof-rendered before/after in headless Chrome (119×119 → 32×32) |
| Larger image tiles per request | ✅ Collage final sizes: single `h-[150px] w-[150px]`; 2-image `w-[255px]`; 3-image `h-[255px] w-[405px]`; 4+ `w-[255px]`; doc/file chips `max-w-[430px]` |
| Send-duplicate preview | ✅ Realtime INSERT handler finds the same-conversation `local_state:'pending'` own message and `removeMessage(s)` it *before* `addMessage(resolved,…)` |
| Remove "Sin contenido" fallback | ✅ `chatService.ts` both sites + `app/(auth)/chat/page.tsx` store empty vs fallback; 0 hits in served bundle |
| Truncated reaction bar | ✅ `overflow-x-auto` on the reaction strip; action menu `maxHeight:'min(430px, calc(100vh - 16px))'` + `overflowY:'auto'`; `ACTION_MENU_HEIGHT_PX` 300→430 |
| Action menu closes when scrolling *inside* it | ✅ Close-on-scroll handler now ignores scroll events whose target is inside the menu (`actionMenuRef` guard) — reaction strip / action list scroll correctly without closing; outer chat scroll still closes |
| Copy message action | ✅ `handleCopy` (Clipboard API + textarea fallback; copies content → attachment URLs → voice URL) + `Copy` button in the action menu |
| Forward message action | ✅ New `ForwardModal` (chats/contacts tabs, search, message preview chip) → then enhanced to **multi-recipient** selection with check-circles + highlighted rows, footer `{n} selected`, and a **Send button icon → spinner → green check "Enviado"** confirmation (800 ms) before closing; contacts reuse an existing direct when present else create `DIRECT`; **preview renders Lexical HTML as natural language (`htmlToText`), HTML formatting still preserved in the sent copy, section divider borders removed, forwarded copies flagged `is_forwarded`** |
| Forwarded-message badge | ✅ New `is_forwarded` boolean column (`20260901_chat_messages_is_forwarded.sql`) set on every forwarded copy; bubble badge shows a small **"Reenviado"** label **with a `Forward` icon** above the content on **both the sender's and the receiver's bubbles** (WhatsApp-style; previously receiver-only — hidden on the sender's own sent bubbles, which just looked like a silent miss since the sender's own copy never flagged it) (`i18n` `forwarded` key, en/es) — always false/null for normal sends |
| Language selector UI | ✅ `ChatHeader` language button (lucide `Languages` icon → dropdown with **Español / English** + green check on active) persists via `updateGlobalPreferences({ locale })` **and** `localStorage 'chat-locale'`; `useTranslations` re-resolves everywhere (header, composer, popovers) and the spell-check engine hot-reloads the matching dictionary |
| Composer spell-check (Spanish/English) | ✅ Hunspell via `nspell` (offline, dictionaries served from `public/dictionaries/{en,es}.{aff,dic}`), lazy loaded per locale; dental/clinical terms seeded (`DENTAL_TERMS`) + persisted personal/ignored words (`chat-spell-personal` / `chat-spell-ignored`); waved-underline marks on misspelled words inside the Lexical composer (`.chat-spell-error`), native spellcheck ON while a dictionary loads; **click a marked word → popover** with up to 6 suggestions (click-to-replace) + **Ignorar** / **Añadir al diccionario** |
| Reply bubble shows image snippet | ✅ In-thread reply quote renders a 24px rounded thumbnail when the quoted message has image attachments (`replyPreviewThumb`); the composer "Replying to…" strip also shows a 32px thumbnail |
| Optimistic progress pill only for media | ✅ `local_state:'pending'` pill gated to `image`/`file`/`voice` (no pill on text sends) |
| Attachment tray closer to bottom | ✅ Root `pb-[max(1.6rem,8%)]` → `pb-[max(1.28rem,6.4%)]` (composer ~20% lower) |
| Notification shows raw `<p>…<span>…</span>…</p>` HTML | ✅ `chatService.sendMessage` notification `message` now runs through `stripHtml()` (regex tag-strip) so in-app/browser notifications store plain `Hello!` (new messages only; old rows still hold HTML) |
| Single `^` toggle per message | ✅ Chevron inside each bubble (sender + receiver), revealed on hover; usable on touch (`opacity-40` default) |
| Replace reaction-tray `+` trigger and inline action buttons | ✅ One popover: quick reactions + divider + frequent + `+` full picker, then Reply/Edit/Delete |
| Chevron placement consistent | ✅ **Inside** the text bubble after content, uniform `gap-1.5` character gap — same for all messages |
| Dynamic above/below positioning | ✅ `openActionMenu` measures row vs scroller (`ACTION_MENU_HEIGHT_PX = 220`); `group` class on rows |
| Delete own messages only (confirm) | ✅ `mine`-gated with `window.confirm` |
| One reaction per user per message | ✅ Reused `handleToggleReaction` |
| i18n | ✅ `moreActions` + `replyingTo` keys added (`en`/`es`) |
| Wire stubbed `Reply` menu item to real reply flow | ✅ Reply sets `ChatPane.replyTo` → quote bar in `Composer` → `reply_to_id` on send; clears after send + on conversation switch (`23d4563`) |
| Show reply preview inside the sent bubble | ✅ `Sender: snippet` bar resolved from server `reply_to` embed **or** locally from the loaded list (realtime / fresh-send fallback) (`9455116`) |
| Click reply preview → jump to original | ✅ Smooth scroll to `[data-message-id]` + 1.6s highlight ring on the target row (`9455116`) |
| Clear composer after send | ✅ Optimistic clear before the async send (`35b3bdf`) |
| Emoji insertion in composer | ✅ 64-emoji picker (`Smile` toolbar button) inserting at cursor (`35b3bdf`) |
| Persistent per-user unread badges | ✅ Unread computed from `chat_participants.last_read_at` in `getConversations`; mark-as-read on realtime delivery while the conversation is open (`35b3bdf`) |
| Fix `PGRST200` on the reply select (`sender:users` embed, no FK) | ✅ Dropped invalid embed; sender resolved client-side from store (`e2aaa6b`) |
| Vercel production deploy | ✅ `eslint.ignoreDuringBuilds` + 8GB build heap; `vercel --prod` green (`6475b67`, `1e9ffbe`) |
| Invalid `GET_LOGS` route export | ✅ Moved store to `src/lib/file-access-log-store.ts` + real `/logs` route (`16600bb`) |
| Verification gate | ✅ `npx tsc --noEmit` clean + 0 ESLint errors |
| Virtualize `<MessageList>` (Phase 4) | ✅ react-window v2 (`List` + `useDynamicRowHeight` + `useListCallbackRef`); per-conversation remount via `key`; scroll-to-latest; jump-to-original uses `scrollToRow` (`021ad84`) |
| Read-receipts (Phase 4) | ✅ `chat_message_reads` table (`message_id`+`user_id` UNIQUE, `delivered_at`/`read_at`), live via realtime channel; WhatsApp-style ✓/✓✓ (grey delivered / blue read) on my sent messages **plus** stacked reader avatars on my latest message (`participantUserIds` from store for the final check) |
| WhatsApp UI tweaks | ✅ Group conversations show the multiple-users icon on a gray circle in the list + header (fallback only when no group `avatar_url`); no-chat-selected panel hides the header/composer and shows the WhatsApp default copy ("Tap a chat to start messaging." / "Selecciona un chat…") |
| Full emoji library | ✅ `src/chat/data/emojiLibrary.ts` generated from `@emoji-mart/data` (1870 emojis, 8 WhatsApp-style categories); shared `EmojiPicker` (category nav + sticky headers) used by the composer and the reaction picker |
| Chat-list delivery ticks | ✅ WhatsApp ✓/✓✓ (grey delivered / blue read) rendered under the timestamps in the conversation list via shared `getMessageReadStatus`; `getConversations` attaches reads to `last_message`, store `upsertMessageRead` refreshes the list-live even before a conversation is opened |
| Read-checkmark blue fix | ✅ Root cause: Realtime UPDATE payloads only carry PK + changed columns (default replica identity), so `chat_message_reads` delivered→read events lacked `message_id`/`user_id` → dropped by `normalizeRead`. Fixed with `database/migrations/20260828b_chat_message_reads_replica_identity.sql` (`REPLICA IDENTITY FULL`) **plus** a client fallback in `useChatRealtime` that re-fetches the row by `id` when the payload is incomplete (works on the current deploy immediately). Also hardened the `chat_messages` UPDATE handler to stop clobbering reactions/voice metadata with `undefined` on edits |
| Phase 2 typing indicator | ✅ `<TypingIndicator>` above the composer (WhatsApp-style animated dots + "Name is typing…" / "Name1, Name2 are typing…" / "Name + N more"), driven by the already-wired presence broadcast (`typing` store map); header + sidebar list now show the named label via shared `getTypingLabel`; i18n keys `typingMulti` / `typingAndMore` |
| Chat attachment upload fix | ✅ Root cause: `app/api/chat/upload/route.ts` tried to `createBucket` at runtime with the **anon** client (`src/lib/supabase/server.ts`) — anon cannot list/create buckets under `storage.buckets` RLS → `listBuckets()` returned `[]` → `createBucket` → "new row violates row-level security policy" → 500. Removed the runtime bucket-ensure; provisioning is via migration. Added `database/migrations/20260828c_fix_chat_uploads_storage.sql` (idempotent): ensures the bucket is public / any mime type (was images+PDF only, blocking .docx/.xlsx) / anon + authenticated `storage.objects` policies |
| Bubble read-check double ✓ (blue) | ✅ `MessageList.renderReadStatus` only rendered the 2nd check when `delivered`, so a **read** bubble showed a single blue ✓ while the chat list showed two. Now `status !== 'sent'` renders both checks (blue when read, grey when delivered) |
| WhatsApp-style attachment tray | ✅ New `AttachmentTray` component: selecting files (paperclip or drag&drop) populates a preview tray above the composer (image thumbnails / document icon + name + size), with chevron navigation + `n/N` counter for multi-file picks and per-file remove; the text input stays editable below (caption). Uploads moved from selection-time to **send-time** (staged `PendingAttachment` with object URLs, revoked on remove/clear/unmount/conversation switch) |
| Stale-participant reads fix | ✅ Root cause: `getMessageReadStatus` counted **every** non-self read row for `need` (`Math.max(otherParticipantCount, readCount, deliveredCount)`), so stale `chat_message_reads` rows from users who are no longer participants (e.g. removed/legacy users) inflated `need` to 2 in a direct chat — the partner reading only satisfied 1 → bubble/list stayed grey "delivered" forever, "won't change to blue". Now it classifies **only against the current other participants** (`otherParticipantIds`, filtered by `Set`); `need` = participant count, stale rows ignored |
| Open-chat scroll-to-bottom fix | ✅ Root cause: `react-window` renders rows top-first using the default estimated height (`48px`) and measures real heights asynchronously (`ResizeObserver`); the mount `scrollToRow` computed the offset from **estimated** heights — short chats even resolved `scrollTop: 0` → the chat opened at the top. `pinListToBottom` re-pins `scrollTop = scrollHeight` each frame until the measured heights integrate and the bottom stays settled for ≥8 consecutive frames (aborts on user wheel/touch, 3 s cap) — verified in headless Chrome against the exact `react-window` build for both 10-row and 200-row chats: `scrollTop === maxScrollTop` and the last row sits at the viewport bottom |
| Soft-delete tombstones + optimistic delete | ✅ 6-commit chain: optimistic delete before server call, tombstone `id` so realtime/re-insert cannot re-add it, refresh list + store after delete, flush mention dropdown, never re-add deleted rows (`90cd5c8`/`1a05136`/`6c4396a`/`c3bef4d`/`55a3cf3`) |
| REPLICA IDENTITY FULL on `chat_messages` | ✅ `23a747a` — soft-delete UPDATEs now carry full row payload to recipients, so deleted messages disappear from every open chat immediately without client re-fetch |
| Deleted-message unread badge + bubble whitespace | ✅ `07518d9` — drops unread count when the latest message is deleted; strips edge whitespace from bubbles to avoid phantom padding |
| Sidebar last-message deletion handling | ✅ `7a1573d` + `e2c3ae2` — excludes soft-deleted rows from sidebar preview and updates preview when the last message is deleted |
| Lexical whitespace runtime fix | ✅ `a3ace91` — `getAllTextNodes` trim prevents Lexical `$createTextNode` crash on empty whitespace nodes |
| Edit composer formatting menu + viewport clamp | ✅ `MessageList.tsx` `openEditMenu` now uses the same above/below viewport-aware positioning as the per-message action menu; fixed overlay at `z-50`; adds **text color** and **highlight** swatches to the edit formatting menu; preserves original HTML formatting in edit textarea |
| Chat list file/image previews with text snippet | ✅ `chatService.ts` fetches `chat_attachments(*)` with `last_message`; `ConversationListItem.tsx` renders image thumbnail + text snippet for image/file messages instead of generic "Image"/"File" labels |
| Supabase singleton client pattern | ✅ Module-level cached instances (`supabaseInstance`, `realtimeSupabaseInstance`) with typed factory functions in `src/lib/supabase.ts` — eliminates "Multiple GoTrueClient instances detected" browser console warning |
| SSR-safe localStorage in i18n hook | ✅ `useTranslations.ts` guards `localStorage.getItem('chat-locale')` with `typeof window !== 'undefined'` — fixes `ReferenceError: localStorage is not defined` during server-side rendering |
| SpellCheckNode exportDOM clean | ✅ `SpellCheckNode.exportDOM` now emits plain TextNode (no decoration), `.chat-spell-error` CSS scoped to `[data-chat-composer]`, `purifyHtml` strips stray classes — red squiggle no longer leaks into rendered message bubbles |
| Composer text color + highlight | ✅ `ColorButtons.tsx` — shared color/highlight swatch popover with viewport-boundary flip; used by both `Composer.tsx` and edit menu |
| Image editor (crop/tilt/draw) in attachment tray | ✅ New `ChatImageEditor.tsx` (crop via `react-easy-crop`, tilt/rotate, draw pencil with color/size/undo); `AttachmentTray.tsx` opens it on the pencil button for staged images, replaces preview on apply |
| Pencil/edit tools missing in attachment tray | ✅ Root cause: the image wrapper `<div className="relative">` had no height constraint, so a tall/portrait image sized it beyond the tray's height and the outer `overflow-hidden` **clipped the pencil** (`absolute bottom-3 right-3`) out of view. Wrapper now `relative flex h-full w-full items-center justify-center` → image is contained and the pencil stays visible |
| Drawing reduced / misplaced after applying edits | ✅ Root cause: strokes were recorded in the cropper's `mediaSize` space but composited with react-easy-crop's `croppedAreaPixels` (rotated **natural** px) — plus the drawing canvas assumed the media fills the whole container when it is actually contained (letterboxed). Added `getMediaMapping` that reads the live on-screen media `<img>` rect and maps container px ↔ rotated natural px, so strokes land exactly where drawn |
| Drawing disappears when the image is sent | ✅ Same coordinate fix: strokes were pushed off-canvas (negative coords from `(p - area)` where the default center-square crop had `area.x/y > 0`). With strokes now in natural-pixel space (same as `area`), they composite inside the crop and survive send (`ChatPane.handleSend` uploads the edited `att.file`) |
| Wide image sides cropped by the square editor | ✅ Root cause: `Cropper` used a hardcoded `aspect={1}` square crop area → wide images were cover-scaled and their sides fell outside the crop window. `aspect` now tracks the media's natural aspect (`size.naturalWidth / size.naturalHeight` from `onMediaLoaded`), so the full image fits and the default crop = entire image |
| Image shrinks ~50% when re-editing | ✅ Root cause: `onMediaLoaded` override `setZoom((size.width * 0.66) / size.height / 2)` — for a 3:2 image that equals `zoom = 0.5`. Removed; `zoom` stays 1 (full fit) unless the user zooms manually in crop mode |
| Selected pen color not highlighted | ✅ Color swatch now uses `ring-2` (white ring + scale on selected, hover ring otherwise) plus a centered `Check` icon — clear focus on the chosen color even for the default white swatch |
| Media/file lightbox only covers the message area | ✅ Lifted the lightbox state from `MessageList` up to the `ChatPane` root and render `<MediaLightbox>` there (`absolute inset-0 z-50` over the whole `relative` pane) — image/file preview now covers **the entire chat pane including the header and the text composer**, not just the message list. `MessageList` delegates tile taps via an `onOpenLightbox` prop (media-only attachment filter moved to `ChatPane`) |
| Edit message via the bottom composer (replaces inline bubble editor) | ✅ Migration complete: `ChatPane` owns `editingMessage`; `MessageList` "Edit message" calls `onEditMessage`; `Composer` shows an amber **"Editing message"** banner (i18n `editingMessage`, en/es), loads the message HTML into Lexical (**formatting preserved**, caret at end), swaps Send→green ✓ while editing, disables paperclip/mic during edit, Enter/✓ commits (optimistic store update + `ChatRepository.updateMessage`, sets `is_edited` + `updated_at`), X/Esc cancels and **restores the pre-edit draft** (`EditSessionRef`). Removed `MessageList`'s inline `EditRow` + chevron formatting popup (Bold/Italic/Underline/Strikethrough/Code/color/highlight) and its ~370 lines. Net diff +206/−380 |
| Spell-check red squiggle leaking into message bubbles | ✅ Root cause: `LexicalNode.exportDOM` calls `createDOM` via **dynamic dispatch**, so the `SpellCheckNode` baked `chat-spell-error` class + inline red `border-bottom` into the **stored message HTML** (only visible when a user types a misspelled word). Bubbles render that HTML via `dangerouslySetInnerHTML` and the global `.chat-spell-error` rule painted the squiggle in received bubbles. **Fixed 3 layers:** (1) `SpellCheckNode.exportDOM` now exports a *plain* `TextNode` (same text/format/style, no class/style) so new content stays clean; (2) `.chat-spell-error`/`-active` CSS scoped to `[data-chat-composer]` only (second line of defense); (3) `purifyHtml` strips any stray `chat-spell-error` class so already-persisted messages render clean |
| Patient Contact +504 phone + WhatsApp | ✅ `formatPhoneDisplay`/`createWhatsAppUrl` (`src/utils/phoneUtils.ts`, patient `codigopais:'504'`), WhatsApp-green `#25D366` "Llamar"→`wa.me` redirect + inline `WhatsAppIcon` SVG, `whatsapp` i18n key (`9567f8b`); a separate plain-call button keeps `tel:` |
| Remove Cédula / Alergias, show Edad instead | ✅ `9567f8b` dropped the Cédula row from the consent body; `db4c2e6` replaces Alergias with **Edad** (`getPatientAge` from `fecha_nacimiento`, falling back to `patients.edad`) in the sent bubble + preview modal |
| Treatment summary: drop Código, add Pago, date-only | ✅ `db4c2e6` — `TreatmentRow.payment` computed as `precio_final × cantidad`; bubble table headers now Fecha/Procedimiento/Cant./Pago (was Código); payment shown in preview too; treatment + odontogram dates stripped to `YYYY-MM-DD` |
| Composer attachment buttons Spanish + one-line left-align | ✅ `db4c2e6` — `t('patientContactCard')`/`t('treatmentSummaryCard')`/`t('odontogramSnapshotCard')` (es default) with `whitespace-nowrap text-left` + `flex-shrink-0` icons so "Resumen de Tratamientos" no longer wraps |
| Odontogram FDI layout + arch order | ✅ `db4c2e6` — bottom arch now `48→41` then `31→38` (correct hemisection order); this commit: 4 quadrant groups (18..11 \| 21..28 / 48..41 \| 31..38) visually separated by a divider, child arch (`55..51` etc.) supported |
| Odontogram tooth shows single flat status | ✅ **This commit** — root cause: `buildTeethStatus` reduced each tooth to one dominant status. `ToothStatus` now carries `central` + `cuadrantes` (mesial/distal/buccal/lingual) + `nota`, each zone as `{estado, color}`; `OdontogramMini` renders a **5-zone SVG tooth** (4 pie slices + center circle + red nota dot) exactly mirroring the pilot's `ToothSvg`. Old baked single-status cards still render via legacy fallback |
| Odontogram counts per state missing | ✅ **This commit** — `buildOdontogramStateCounts` aggregates zone statuses per tooth (one `sano` per all-healthy tooth, unique non-sano zones counted, matching pilot `contadorEstados`) and `OdontogramMini` renders a `Label: N` swatch legend |
| Commit | ✅ `021ad84` (virtualization), read receipts committed with it; checkmarks + read-receipt pipeline `99815aa`; group icon + default panel `7a5e89a`; full emoji library + list ticks `ce5d6c6`; blue read-checkmark fix + Phase 2 typing `d34d98e`; chat attachment upload fix `05a0634`; bubble double-blue ✓ + WhatsApp attachment tray `99961bd`; stale-participant reads fix `aa5ead4`; open-chat scroll-to-bottom + action menu pinning `756e854`; **copy/forward + media progress + echo dedupe + Tailwind `src/` fix + tile sizes + tray inset `41633ad`**; **Phase 1 keyboard-nav + responsive sidebar, Phase 3 lists/code/@mentions/draft, forward/reply/notif fixes `e3f52c4`**; **delete tombstoning + optimistic delete + mention refresh `c3bef4d`/`55a3cf3`/`1a05136`/`6c4396a`/`90cd5c8`**; **sidebar last-message deletion fixes `7a1573d`/`e2c3ae2`**; **deleted-message unread badge + bubble whitespace `07518d9`**; **REPLICA IDENTITY FULL `23a747a`**; **Lexical whitespace fix `a3ace91`**; **react-easy-crop CSS import `c098bb9`**; **image-editor rendering fixes `88651cc`**; **full-pane media/file lightbox + ForwardModal natural-language HTML preview (dividers removed) + `is_forwarded` receiver-only "Reenviado" badge `ae4774d`**; **wallpaper multi-design `16488fc`**; **per-conversation chat settings scoping `2c20f13`**; **edit-composer migration + spell-squiggle bubble leak fix**; **patient-card attachments `3696874` (shared bubble) + `33f7946` (array-normalization root-cause fix) + `9567f8b` (phone/WhatsApp/arch) + `db4c2e6` (Spanish buttons, Edad, Pago, arch order) + this commit (5-zone odontogram mini + quadrant separation + per-state counts)** |

### Work State

**Completed**
- **Phase 0 (~100%)** — tooling, `/src/chat` structure, repository/service layers, ESLint/tsc gate.
- **Phase 1 (~100%)** — keyboard navigation in the conversation sidebar (ArrowDown/Up/Home/End via `handleListKeyDown` + `scrollIntoView`, `<li role="presentation"><button role="option" aria-selected data-conv-index>`, `onKeyDown`/`buttonRef` threading); **responsive `<md` sidebar** — `sidebarOpen`/`closeSidebar` in `ChatLayout`, fixed `z-30` overlay (`md:hidden`), collapsible `md`-only transform (`-translate-x-full md:static md:translate-x-0`, `z-40`), auto-close on selection on mobile, mobile-only `Menu` hamburger in `ChatHeader`.
- **Phase 2 (100%)** — realtime consolidated in `useChatRealtime` (messages/conversations/participants/`chat_message_reads`/presence/typing broadcast); `reactions JSONB` + voice migration applied (`database/migrations/20260827_chat_extensions.sql`); **server-computed per-user unread counts** (`getConversations`); **`chat_message_reads` table + read-delivery upserts** (`markConversationRead`/`markDelivered`); **typing indicator UI** (`<TypingIndicator>` above composer + named labels in header/list, see log below); **REPLICA IDENTITY FULL** on `chat_messages` for realtime soft-delete delivery (`23a747a`).
- **Phase 3 (≈100%)** — Lexical composer (bold/italic/underline), **lists/code toolbar** (`@lexical/list` unordered/ordered + `@lexical/code-core` `CodeNode`), **@mentions** (`MentionNode` DecoratorNode → `<span class="chat-mention">`, `MentionPlugin` @… dropdown with Arrow/Tab pick + commit), drag-&-drop + multi-file upload, voice notes, **full emoji picker** (1870 emojis, category nav, sticky headers — also used for reactions), **optimistic send-clear**, reply-quote bar, **staged attachment tray with image editor** (crop/tilt/draw), **text color + highlight swatches** (`ColorButtons.tsx`).
- **Phase 4 (≈100%)** — grouping, reactions (one per user), unified hover action menu (above/below positioning, **Copy/Forward/Reply/Edit/Delete**), **composer-based editing** ("Editing message" amber banner + green ✓ confirm, Lexical formatting preserved, X/Esc cancels and restores the pre-edit draft; the old inline bubble textarea + chevron formatting menu was removed), quote/reply preview **with image snippet** + click-to-jump, read-receipts + ✓/✓✓, **sidebar last-message previews** with image thumbnails + text snippets, **forward modal (natural-language HTML preview, no divider borders) + receiver-only "Reenviado" bubble badge (with forward icon)**, **WhatsApp-style sticky date pills** (localized Today/Yesterday/`Month Day (Year)` via `src/chat/formatChatDate.ts`; floating overlay driven by the virtualized list's `onRowsRendered` so the pill always mirrors the date group at the top of the viewport).
- **Phase 7 (≈100%)** — i18n layer (`en`/`es`, typed `TranslationKey`) fully wired with all keys (`moreActions`, `replyingTo`, `typingMulti`, `copyMessage`, `forward*`, `forwarded`, `editorCrop`, `editorTilt`, `editorDraw`, `language*`, `spell*`, etc.), **language selector in `ChatHeader`**, and a **composer spell-checker** (`nspell` + per-language dictionaries, squiggles + click-for-suggestions popover, personal dictionary + ignore persistence). **Theming finished (this commit):** design-token system (`app/color-system.css` — `--fd-*` vars, light + dark, imported from `app/layout.tsx`) powering accent semantics (`.fd-accent-bg/-text/-soft-bg/-soft-border/-ring`, chat mentions), bubble backgrounds + auto-contrast text via CSS vars set on the `ChatLayout` root (`[data-chat-root]` inline `--fd-accent`/`--fd-bubble-*`; `bubbleTextColor` in `utils.ts` picks dark/white by luminance when the setting is Auto/empty); **new `chat_settings` columns** `text_size` (sm/md/lg), `density` (comfortable/compact), `accent_color`, `my_text_color`/`other_text_color` (empty = Auto) behind `20260904_chat_settings_phase7.sql`, conversation-scoped → global → defaults like wallpapers; **Chat Settings panel** gains segmented text-size/density pickers + accent + my/other text-color swatch sections (all accent-token highlighted); font-size scales bubbles + composer via `--fd-text` (`[data-text-size]`), compact density tightens `[data-message-row]` rows (`[data-density='compact']`); read-status ticks, reply/quote strip, send-progress chip, reaction pill/active state, unread highlight + read-receipt title tooltips (`statusSent/Delivered/Read`) all accent-token driven; **i18n gaps closed** — MentionPopover actions (`mentionText`/`mentionCall`/`mentionVideo`/`mentionAlert`), composer format tooltips (`formatBold/Italic/Underline/List/OrderedList/Code/Emoji`), `editImage`, `playVoice`, `loading`; dark-mode polish for mentions/read-ticks/reactions/reply strip. Entire `src/chat/**` re-audited: zero `blue-*` classes left (accent tokens everywhere), `bg-white/*` remaining only as intentional overlays on black/photo surfaces. tsc + scoped ESLint clean.
- **Phase 8 (a11y, ✅ audited + finished `4d35fb1`)** — `aria-live="polite"` new-message region in `MessageList` (skips announcements when thread focused); reusable **`useFocusTrap`** on every modal/menu/picker; ARIA labels on title-only buttons; `role="menu"/"menuitem"` action tray + `aria-expanded`, `role="dialog" aria-modal aria-labelledby`, `role="toolbar"` composer; skip-to-content link (`#chat-messages`); shortcuts `Ctrl+K` / `Ctrl+Shift+M` / `Alt+↑↓`; `:focus-visible` accent outline scoped to `[data-chat-root]`. **Audit finish `4d35fb1`**: static axe-equivalent pass over `src/chat/**` (verified: landmark/labels/roles/`aria-live`/skip-link; every icon-only button has `aria-label`+`title`; clickable rows are real `<button>`s) → added `role="main"` on `ChatLayout`, dynamic `<html lang>` synced from the resolved chat locale (`DocumentLang`, es-first server default — was hardcoded `en`), and bumped empty-state/instructional grays to AA-tolerable contrast (ChatPane no-conversation, MessageList empty, Sidebar empty + new-chat modal, TypingIndicator). Live axe/Lighthouse + NVDA/VoiceOver run — **done in Phase 10 (T31–T32 ✓, 2026-09-07)**.
- **Date pills (committed `e7c23b4`)** — WhatsApp-style sticky date labels (`src/chat/formatChatDate.ts` `getChatDateKey`/`getChatDateLabel`; i18n `dateToday`/`dateYesterday`); floating overlay mirrors the date group at the top of the virtualized viewport via react-window `onRowsRendered`; sender/avatar grouping also breaks on local-day boundaries.
- **Chat-settings persistence bug (committed `8ad8ba9`)** — `load()`/`update()` now discard stale responses that no longer match the active (user, conversation) scope, so the mount-time global load can no longer wipe a chat's wallpaper/bubble styles; per-scope cache repaints revisits instantly. Persistence remains Supabase (`chat_settings` table + `chat-wallpapers` bucket), not localStorage.
- **Production** — feature flag activated (`NEXT_PUBLIC_USE_NEW_CHAT=true`), Vercel deploy green, soft-delete tombstones + optimistic delete, REPLICA IDENTITY FULL, deleted-message unread badge fix, sidebar last-message deletion fixes.
- **Critical bug fixes (this commit)** — **Supabase singleton pattern** (eliminated "Multiple GoTrueClient instances" warning), **SSR-safe localStorage** (guarded i18n hook), **SpellCheckNode exportDOM clean** (red squiggle leak fixed).
- **Patient-card attachments (this batch)** — chat patient-case cards embed real data at send time: patient snapshot (`patients(*)` embed in `chatService`/`useChatRealtime`), treatment rows + per-line totals (`buildTreatmentRows`), active-odontogram snapshot; functional actions (view profile `/patient-preview/[id]`, odontogram `/odontogram-pilot?id=`, `tel:` call, WhatsApp `wa.me`, treatment+odontogram PDF); contact card shows `+504` phone + WhatsApp-green button; consent body shows doctor + **Edad** (no Alergias/Cédula); treatment table Fecha/Procedimiento/Cant./**Pago**; **odontogram mini chart** renders **5-zone SVG teeth** (quadrant slices + center + nota dot), **quadrant separation** with a divider, and a **per-state count legend** (`OdontogramMini` + zone-aware `ToothStatus`/`buildOdontogramStateCounts`); attachment buttons Spanish + single-line (`db4c2e6` + HEAD).

 - **Phase 9 (perf, ✅ `4099e90`)** — bundle split: `app/(auth)/chat/page.tsx` rewritten as a **1.37 kB `next/dynamic` loader** for both `ChatLayout` and the legacy monolith (moved verbatim to `src/chat/monolith/ChatPageMonolith.tsx`, gated by `NEXT_PUBLIC_USE_NEW_CHAT`); in-tree lazy chunks via `next/dynamic` `ssr:false` for **Composer (Lexical + nspell, `ComposerFallback` loading), EmojiPicker, AttachmentTray (react-easy-crop `ChatImageEditor`), MediaLightbox, ChatSettingsPanel, ForwardModal, PatientSelectionModal, PatientCardPreviewModal**; chat-only CSS moved to `src/chat/chat.css` loaded only by the `/chat` route via new `app/(auth)/chat/layout.tsx` (this also fixed a pre-existing broken `.animate-pulse-text {` brace in `globals.css` that had swallowed typing-dot/mention/spell-check rules); `@next/bundle-analyzer` wired (`ANALYZE=true`). Verified: prod build green, `/chat` First Load JS now ~105 kB total (incl. 104 kB shared) with the route's own chunk at **1.37 kB**, chat shell ≈ 94 kB + separate monolith/composer/emoji/editor chunks. Prod-image flag note: `NEXT_PUBLIC_USE_NEW_CHAT=true` surfaces the old gated default in the deployed image — verify the new UI at runtime after the next production deploy (see Blocked).
- **Phase 5 (push, ✅ this commit)** — VAPID web-push infrastructure: `web-push` npm package installed; VAPID keypair in `.env.local`; generation script (`scripts/generate-vapid.mjs`). **`push_subscriptions` table** (`user_id TEXT`, `endpoint UNIQUE`, `p256dh`, `auth_secret`) + RLS + service-role API writes — `20260906_push_subscriptions.sql`. **6 API routes** (`vapid-public-key`, `subscribe`, `unsubscribe`, `test`, `send`, `webhook`); webhook route validates `PUSH_WEBHOOK_SECRET`, resolves participants, resolves sender name via `clerkClient`, strips HTML from Lexical content, skips muted participants. **Service worker `push` handler** (`public/sw.js`): parses JSON payload, suppresses tray when a focused window client is already on the target conversation (avoid spam); `notificationclick` navigates to `/chat?conv=` and posts `NOTIFICATION_CLICKED`. **`usePushNotifications` hook**: status machine (unsupported/default/denied/enabled/subscribed), permission → PushManager.subscribe → server upsert lifecycle, test notification. **ChatSettingsPanel Notifications section**: Bell icon, enable/disable toggle, test button, status labels (en/es). **Client-side hidden-tab trigger** (`src/chat/push/triggerHiddenTab.ts`): called by `ChatLayout` when a realtime incoming message arrives while `document.hidden`; strips HTML, POSTs `/api/push/send` (authed → pushes to caller's own subscriptions → OS tray). **`?conv=` deep-link** via `<Suspense>`-wrapped `DeepLinkEffect` using `useSearchParams()` + `message` event listener for `NOTIFICATION_CLICKED` from the SW; SW registered globally in `ChatLayout` (desktop included). **Optional pg_net trigger** (`20260906b_chat_messages_push_webhook.sql`): PL/pgSQL fires `net.http_post` on every `chat_messages` INSERT → `/api/push/webhook` (delivering notifications when no tab is open). Requires pg_net extension + editing `_base_url`/`_secret` in the SQL. `stripHtml` utility added to `@/chat/utils`.
- **Phase 5 push — completion + live verification (commits `66e83e4` → `6f2e9cf`):** server delivery chain healthy on prod (pg_net trigger **enabled**, test user `user_3A1mYfR054eV3tqtellpfMKZ7f6`, 3 subscriptions, `last_success_at` current) and **device-tested working** (Chromebook PWA cold-boot, mobile PWA, fully-closed app). SW rewritten to **per-thread aggregated WhatsApp-style notifications** (`6f2e9cf`, CACHE_NAME v10): native IndexedDB (`diamond-link-push/threads`), one tray card per chat (`tag:'chat-thread-<id>'`, `renotify`), title `Nombre (N)`, last-3-snippet body, group snippets prefixed by sender; actions **Responder** (open thread + focus composer) / **Marcar leído** (clear badge); `notificationclick` clears the queue, targets the open `/chat` window else `openWindow('/chat?conv=<id>')`. **Every push must end in `showNotification`** (`8e8c452`) — silent substitutions exhaust Chromium's budget and the worker stops waking up entirely. Bug fixes: `66e83e4` pg_net `headers` = named 4th arg; `6dfebbc` sender avatar/actions + subscribe-chatter; `f2bf1f2` cold-boot notification tap no longer bounces to sign-in (`/chat` exempted in `middleware.ts` + signed-out screen in `ChatLayout`); `fbcac20` React #185 from the `?conv=` deep-link (idempotent `setSelectedConversation` + `DeepLinkEffect` guard). Webhook + hidden-tab triggers now send structured `data` (`senderName`/`messageText`/`conversationName`/`conversationType`/`url`).

 **Active**
- Commit `8f6a4fa` (pushed) contains: image editor (`ChatImageEditor.tsx`), text color/highlight (`ColorButtons.tsx`), attachment tray integration, edit menu fixes, draft persistence, lists/code/@mentions, reply quote with image snippet, notification HTML strip, sidebar previews, soft-delete chain and REPLICA IDENTITY FULL.
- `rendererStyled` in `utils.ts` now typed with `ChatPurifyConfig` to support `ALLOWED_STYLE_PROPERTIES` for color/highlight rendering.
- **Image-editor rendering fixes (committed `88651cc`):** pencil no longer clipped in the attachment tray; drawing strokes recorded from the live on-screen media rect (`getMediaMapping`) so they composite in the right position/size; crop `aspect` now = the image's natural aspect (no square side-cropping, no crop offset shift); removed the `setZoom` on-load hack (caused 50% re-edit shrink); selected pen color shown with ring + check icon.
- **ForwardModal + full-pane lightbox (committed `ae4774d`, head `f14cb8a`):** forward preview shows Lexical HTML as natural language (`htmlToText` from `@/chat/utils`) while the sent copy keeps its HTML formatting; the modal's section divider borders (header/tabs/search/error/footer) removed; every forwarded copy is persisted with `is_forwarded=true` (new `20260901_chat_messages_is_forwarded.sql` migration; `ChatMessage`/`CreateMessageData` type fields; `chatService.sendMessage` insert) and the bubble badge renders a small **"Reenviado"** label **with a `Forward` icon** above the content (`i18n` `forwarded` key, en/es) on both sender and receiver bubbles; `MediaLightbox` lifted to the `ChatPane` root so the preview covers the header + composer too.
- **Composer spell-check + language selector (committed):** `src/chat/spellcheck/spellcheckService.ts` (per-locale lazy `NSpell` engine over fetched `public/dictionaries/*`, seeded dental terms, localStorage personal/ignored words, failed loads retry up to 10×/2.5s), `SpellCheckNode.ts` (TextNode subclass carrying the `chat-spell-error` class + **inline `borderBottom` style** via `createDOM` so the red underline renders reliably even when CSS caching is stale; default-arg constructor so the fork's static `importJSON` dev check passes), `SpellCheckPlugin.tsx` (debounced 150ms guarded re-mark pass on every editor update that **never rebuilds the node under the caret** — anchored nodes are skipped, and the completed-words-before-caret case is handled by `splitText` at the current word start so the caret's own node is never replaced; document-level `mouseup` listener with root-containment check opens the word popover with suggestions / Ignorar / Añadir al diccionario; `onReady` toggles native spellcheck; engine-load retry + `window.__spellDebug` console surface for diagnosis); wired into `Composer.tsx` (`nodes` + `<SpellCheckPlugin locale={locale}>` + `spellCheck={!spellReady}`); `.chat-spell-error` CSS in `app/globals.css`; language selector (Languages icon + Es/En menu) in `ChatHeader.tsx` persisting to global preferences + `chat-locale`. **Verified end-to-end**: squiggle renders on flagged words in the composer, popover opens on click, personal dictionary persists.
- **Edit-composer migration + red-squiggle bubble fix (this commit):** `ChatPane` now owns `editingMessage` (reset on conversation switch); `MessageList` drops the inline `EditRow`/`EditMenu` (~380 lines removed) and calls a new `onEditMessage` prop; `Composer` gained an `EditSession` — amber "Editing message" banner, `loadHtmlIntoEditor` (message HTML → Lexical, caret at end), Send→green ✓ (`Check` icon), paperclip/mic disabled while editing, Enter/✓ commits (trim whitespace → `$generateHtmlFromNodes` → optimistic `updateMessage` + `ChatRepository.updateMessage`), X/Esc cancels and restores the pre-edit draft. **Red-squiggle leak**: `SpellCheckNode.exportDOM` was inheriting `TextNode.exportDOM`, which dynamic-dispatches `createDOM` → the stored message HTML included the `chat-spell-error` class + red `border-bottom`, and bubbles render content via `dangerouslySetInnerHTML` with the global CSS → squiggled text in bubbles. Fixed by overriding `exportDOM` to emit a **plain TextNode** (no decoration), scoping `.chat-spell-error*` to `[data-chat-composer]` in `app/globals.css`, and stripping the class in `purifyHtml` (covers already-persisted messages). Gate clean (`tsc` + scoped ESLint).
- **Phase 7 theming completion (this commit):** `app/color-system.css` (Fluxer-style `--fd-*` design tokens, light + dark, `.fd-accent-*` utilities); accent-semantic refactor of every remaining `blue-*` class in `src/chat/**` (Settings panel, sidebar search/group-name inputs, direct/group tabs, composer send btn + voice + reply + drag ring, attachment tray, forward modal tabs/chips/done, image editor, mention popover, `.chat-mention` now `var(--fd-accent)`); bubbles/CSS-var theming (`ChatLayout` root `[data-chat-root]` sets `--fd-accent`/`--fd-bubble-mine-bg/text`/`--fd-bubble-other-bg/text`, `bubbleTextColor` auto-contrast when unset; `MessageList` reads the vars, drops its `DEFAULT_*` constants); **four new per-user customizations** (`text_size`, `density`, `accent_color`, `my_text_color`/`other_text_color` w/ Auto) added to `chat_settings` + `ChatSettingsService` types and the **Chat Settings panel** (segmented text-size/spacing + accent + my/other text-color swatch sections); i18n gaps closed; dark-mode polish (mentions/ticks/reactions/reply strip). New migration `20260904_chat_settings_phase7.sql` — **applied + schema-verified** (2026-09-07). Gate clean (`tsc` + scoped ESLint).

**Blocked**
- Full local `npm run build` is **slow (~10 min)** in the dev container — it previously stalled at 4GB heap but completes with the 8GB `NODE_OPTIONS` heap (verified on `db4c2e6`, regenerates `public/sw-precache.json`); the per-commit gate is `tsc` + ESLint, Vercel is the functional build check and is green.
- **~~Phase 5 (push) — runtime verification~~ — resolved.** Push is now live-tested working (Chromebook PWA cold-boot, mobile PWA, fully-closed app); the pg_net webhook trigger is **enabled on the live DB** with the real `PUSH_WEBHOOK_SECRET`, delivery chain healthy. Platform leftovers: iOS Safari renders no notification `actions` (list notification only), and iOS requires Home-Screen install for `PushManager` — not fixable in code.
- ~~**Runtime verification of the new chat UI**~~ — **resolved in Phase 10**: full login → convo → push → PWA flows executed on the deployed image (flag active) via desktop/mobile/PWA device runs. (Note: local builds regenerate `public/sw-precache.json` — revert unless building.)
- ~~**Pending manual Supabase migrations**~~ — **resolved**: all `chat_settings` migrations (`20260903_scoped`, `20260903_wallpaper_style`, `20260904_phase7`) are **applied + schema-verified live** (2026-09-07 — settings persistence working, no 400s; `id`, `conversation_id`, `wallpaper_style`, `text_size`/`density`/`accent_color`/`my_text_color`/`other_text_color` all present). Push stays applied/active: `20260906_push_subscriptions.sql` + `20260906b` pg_net trigger live with the real `PUSH_WEBHOOK_SECRET` (delivery verified).

### Next Move
1. **Phase 10 — ✅ delivered.** Only optional items remain across all phases (none block release): self-host the Inter font via `next/font`, generate the bundle-analyzer HTML report (`npm run analyze`), and a standalone Lighthouse PWA report.
2. Re-generate the odontogram/patient cards **by re-sending them** in the chat (metadata is baked at send time) and hard-reload (Ctrl/Cmd+Shift+R) to bust the service-worker cache after deploy.
3. Keep the gate: `npx tsc --noEmit` + scoped ESLint below.

### Verification Gate
```bash
npx tsc --noEmit
NODE_OPTIONS="--max-old-space-size=4096" npx eslint src/chat --cache --format stylish
```

### Relevant Files
- `src/chat/components/MessageList.tsx` — react-window v2 virtualized list (`List` + `useDynamicRowHeight` + `useListCallbackRef`), grouping, unified action menu (**Copy/Forward/Reply/Edit/Delete**), reactions, reply preview bar **with image snippet thumbnail**, click-to-jump + highlight, read-receipt avatars on last-mine message, action-menu-internal scroll guard (`actionMenuRef`); **"Edit" calls `onEditMessage(msg)` prop** (no more inline bubble editor — the composer is the edit surface); reaction picker uses the full `EmojiPicker`; **media-tile taps delegate to `onOpenLightbox` prop** (lightbox itself renders at the `ChatPane` root); **"Reenviado" label (with `Forward` icon) above content when `msg.is_forwarded` (shown on both sender and receiver bubbles — WhatsApp-style; not gated on `!mine` anymore)**.
- `src/chat/components/EmojiPicker.tsx` + `src/chat/data/emojiLibrary.ts` — full Unicode emoji picker (1870 emojis, 8 categories, category nav + sticky headers); library generated from `@emoji-mart/data`.
- `src/chat/components/ForwardModal.tsx` — **multi-recipient forward modal** (chats/contacts tabs, check-circle selection, footer `{n} selected`, Send button icon → spinner → green-check "Enviado" confirmation; reuses an existing direct or creates `DIRECT`); **message preview strips HTML (`htmlToText`) for natural-language display while the forwarded copy keeps its formatting; section divider borders removed; send sets `is_forwarded:true`**.
- `src/chat/components/Composer.tsx` — Lexical editor, reply-quote bar (`replyTo`/`onCancelReply`, **with thumbnail snippet**), full emoji picker (`Smile`), **lists/code toolbar** (`@lexical/list` + `@lexical/code-core`), **`DraftLoader` draft persistence** (`chat-draft:{conversationId}`), optimistic send-clear, drag-drop upload, voice note, **text color + highlight swatches** (`ColorButtons`); **`EditSession`** — amber "editingMessage" banner above the editor, `loadHtmlIntoEditor` for the message HTML (could include text-color formats), Esc-via-`registerRootListener`/X cancels and restores the pre-edit draft, Send swaps to green ✓ (`Check`) while `editingMessage` is set, attach/mic disabled during edit.
- `src/chat/components/ColorButtons.tsx` — shared color/highlight swatch popover with viewport-boundary auto-flip; used by `Composer` and `MessageList` edit menu.
- `src/chat/components/ChatImageEditor.tsx` — image editing overlay (crop via `react-easy-crop`, tilt/rotate, draw pencil with color/size/undo/apply); integrated into `AttachmentTray` for staged images. **Coordinates:** `getMediaMapping` reads the live on-screen media `<img>` rect to map container px ↔ rotated natural px (cropper's `croppedAreaPixels` space), so drawn strokes render exactly where the user drew and survive applying/sending; `aspect` tracks the media's natural ratio so the full image fits (no square side-crop); pen-color highlight uses ring + check.
- `src/chat/components/AttachmentTray.tsx` — WhatsApp-style attachment preview tray (thumbnails/icon, chevron navigation, `n/N` counter, per-file remove, **image editor pencil button**, composer inset `pb-[max(1.28rem,6.4%)]`) — Phase 3. Image display wrapper is `relative flex h-full w-full items-center justify-center` so the pencil (anchored `absolute bottom-3 right-3`) is never clipped out of view by the tray's `overflow-hidden`.
- `src/chat/components/Sidebar.tsx` — keyboard nav (`listRef`/`itemRefs`/`handleListKeyDown`, Arrow/Home/End + `scrollIntoView`); `ConversationListItem` renders `li>button` with `aria-selected`/`data-conv-index` + `onKeyDown`/`buttonRef`.
- `src/chat/mentionNode.ts` + `src/chat/components/MentionPlugin.tsx` — **@mention**: `MentionNode` (DecoratorNode → `<span class="chat-mention">@Name`), `MentionPlugin` (registerUpdateListener `@query` detection, `position:fixed` dropdown filtered against store users, Arrow/Tab nav, commits `$createMentionNode` + space).
- `src/chat/components/ChatPane.tsx` — `replyTo` state, `handleSend` passes `reply_to_id`, flex root, mark-as-read on conversation open; message-list remount per conversation (`key`); **owns the `editingMessage` state** (reset on conversation switch) and `handleEditConfirm(content, msg)` (optimistic store `updateMessage` + `ChatRepository.updateMessage`, `is_edited`/`updated_at`); wires `onEditMessage`/`editingMessage`/`onCancelEdit`/`onEditConfirm` into `MessageList`/`Composer`; **own the `MediaLightbox` state** and render it `absolute inset-0` at the pane root so the image/file preview covers header + message list + composer.
- `src/chat/spellcheck/spellcheckService.ts` — **`nspell` engine service**: lazy per-locale engine cache (fetches `public/dictionaries/{en,es}.{aff,dic}`), `DENTAL_TERMS` seed set (es+en clinic vocabulary), personal/ignored word persistence (`chat-spell-personal`/`chat-spell-ignored` in localStorage), `SpellcheckHandle` (`correct`/`suggest`/`addToDictionary`/`markIgnored`/`isIgnored`).
- `src/chat/spellcheck/SpellCheckNode.ts` — `TextNode` subclass (`spellcheck-text`) whose DOM carries the `chat-spell-error` class **and inline `borderBottom` style** (red line); plain-text+format transparent; **`exportDOM` overridden to emit a plain TextNode so the spell decoration never leaks into stored message HTML** (class + border-bottom stripped) — the fix for the red squiggle showing in rendered bubbles.
- `src/chat/spellcheck/SpellCheckPlugin.tsx` — Lexical plugin: debounced (150 ms) guarded re-mark pass after every editor update (`isComposing` guard, code/mention subtrees skipped, word-token regex incl. `’ - ·` joins), **click-word popover** (up to 6 suggestions → click to replace via `spliceText`; Ignorar / Añadir al diccionario); `onReady` reports dictionary load so the composer flips native spellcheck during the fetch gap.
- `src/types/nspell.d.ts` — CJS type shim for `nspell` (`export = NSpell`).
- `public/dictionaries/{en,es}.{aff,dic}` — raw Hunspell dictionaries copied from `dictionary-en` / `dictionary-es` (fitted for the browser fetch path; the published packages read files via `node:fs` so they can't be imported in JS).
- `src/chat/components/ChatHeader.tsx` — **language selector** (lucide `Languages` button → Español/English dropdown, check on active; persists `{ locale }` via `useGlobalPreferences` + `chat-locale`).
- `src/chat/hooks/useInstallPrompt.ts` — **Phase 6 install prompt hook**: captures `beforeinstallprompt`, `promptInstall()` returns accepted/dismissed, `installed` detection via `(display-mode: standalone)` + iOS `navigator.standalone` + localStorage, iOS Safari detection (`isIos`) for the Add-to-Home-Screen hint.
- `src/chat/components/InstallAppButton.tsx` — **Phase 6 header button**: Download icon → native install dialog when installable, iOS Safari hint popover, hidden once installed.
- `src/chat/offline/offlineQueue.ts` — **Phase 6 offline send queue** persistence (localStorage `chat-offline-queue-v1`): `enqueue`/`remove`/`list`/`clear`, `isOffline()`. Text-only sends can be queued (file/image/voice needs a live upload).
- `src/chat/hooks/useOfflineQueue.ts` — **Phase 6 queue driver**: connectivity listener (`online`/`offline`), `queuedCount`/`isOnline`, flush on reconnect through `ChatRepository.sendMessage`, routes real messages back via `onSuccess`/`onFailure`.
- `src/chat/hooks/useChatRealtime.ts` — consolidated realtime (messages/conversations/participants/**`chat_message_reads`**/presence/typing) + mark-as-read on delivery while open; **read-receipt fallback** (re-fetch row by `id` when UPDATE payload lacks `message_id`/`user_id`); hardened `chat_messages` UPDATE handler; soft-delete tombstone guard; **`normalizePatientCaseLink()`** on the INSERT re-fetch (patient-case cards resolve their patient/`metadata`).
- `src/services/chatService.ts` — `reply_to:chat_messages(id, content)` select (no `users` embed — no FK), per-user unread counts from `last_read_at`, `getReadsByConversation` + `attachReads`, centralized `markConversationRead` (last_read_at + `chat_message_reads` upserts), `getConversations` attaches reads to `last_message` for list ticks, **`stripHtml` on notification `message`** (no raw Lexical `<p>…` in notifications), **fetches `chat_attachments(*)` with last_message** for sidebar previews; **`sendMessage` insert persists `is_forwarded`**; **`normalizePatientCaseLink()`** fixes the PostgREST **to-many `patient_case_link` array** (`getConversation`/`getMessages`/`sendMessage`) so patient-case cards resolve their patient/`metadata`.
- `src/chat/repository.ts` — data layer (messages, conversations, `markAsRead` → `markConversationRead`).
- `src/chat/store/chatStore.ts` — Zustand store (Phase 1); `users` map for client-side sender lookup; `upsertMessageRead` merges live read receipts into messages **and** the conversation `last_message` (list ticks live even before opening a chat); `tombstoneMessage` prevents deleted rows from re-appearing.
- `src/chat/utils.ts` — shared `getMessageReadStatus` (sent/delivered/read) used by bubbles + chat list; `getTypingUserIds` + `getTypingLabel` (typing indicator names); `getFileKindMeta` for sidebar file-type icons; **`purifyHtml` strips any `chat-spell-error` class attribute** (with `ALLOWED_STYLE_PROPERTIES: ['color','background-color']`) so old/polluted messages render clean; **`bubbleTextColor(bg)`** returns dark `#1f2937` for light bubble backgrounds (luminance > 175) else white — the Auto-contrast path for my/other text colors.
- `src/chat/patientCardData.ts` — **patient-card metadata builder** (`buildPatientCardMetadata` enriches attachment metadata at send time: patient snapshot, treatment rows + totals (`buildTreatmentRows`, line payment = `precio_final × cantidad`), active-odontogram snapshot); `buildTeethStatus` now emits **per-zone** `ToothStatus` (`central` + `cuadrantes` mesial/distal/buccal/lingual as `{estado,color}` + `nota`), `buildOdontogramStateCounts` (zone-aware, one `sano` per all-healthy tooth, mirrors pilot `contadorEstados`), `getPatientAge` (Edad from `fecha_nacimiento`, fallback `pacientes.edad`), `resolveCardPatient` (array-tolerant), exported `ODONT_STATUS_COLORS`/`ODONT_STATE_LABELS` (`Sano/Cariado/Corona/Erosión/…`).
- `src/chat/components/OdontogramMini.tsx` — **odontogram mini chart** used by the sent card + preview: each tooth is a **5-zone SVG** (4 pie slices + center circle, per-zone colors, red nota dot — mirrors `components/OdontogramPreview.tsx` `ToothSvg`); arch rendered as **4 quadrant groups separated by a divider** (adult `18..11|21..28 / 48..41|31..38` + child `CHILD_ARCH`); **per-state count legend** (`Label: N` swatches).
- `src/chat/components/PatientCardBubble.tsx` — sent-card renderer: contact (name + `+504` phone via `formatPhoneDisplay`, WhatsApp `wa.me` button in `#25D366` + `WhatsAppIcon`, email, doctor), consent body with **Edad**, treatment table Fecha/Procedimiento/Cant./**Pago**, odontogram section with `<OdontogramMini>`, `dateOnly()` stamps; action row (profile / odontogram / WhatsApp / PDF).
- `src/chat/components/PatientCardPreviewModal.tsx` — pre-send preview of the same card (loading state while metadata is built, caption input), matching the bubble's Edad/arch/Pago/odontogram rendering.
- `src/utils/phoneUtils.ts` — `formatPhoneDisplay`, `createWhatsAppUrl(phone, messageOrCountryCode, countryCode)`, `countries` list (Honduras `+504`).
- `src/chat/components/Composer.tsx` — patient-card attachment buttons now use `t('patientContactCard')`/`t('treatmentSummaryCard')`/`t('odontogramSnapshotCard')` (es) with `whitespace-nowrap text-left` + `flex-shrink-0` icons.
- `src/chat/i18n/translations.ts` + `useTranslations.ts` — `moreActions`, `replyingTo`, `typing`, `typingMulti`, `typingAndMore`, `copyMessage`, `forward`/`forwardTitle`/`forwardChats`/`forwardContacts`/`forwardSelected`/`forwardNone`/`forwardDone`/`forwardSending`/`forwardFailed`/`forwarded`/`searchRecipient`/`noResults`, `editorCrop`, `editorTilt`, `editorDraw`, `language`/`languageEs`/`languageEn`, `spellNoSuggestions`/`spellIgnore`/`spellAddToDictionary`, **`editingMessage`** (composer edit banner), emoji category names (`en`/`es`).
- `src/chat/components/TypingIndicator.tsx` — WhatsApp-style dots + named label above the composer (Phase 2).
- `src/chat/components/ConversationListItem.tsx` — renders image thumbnails / file-type color icons + text snippets for last-message previews; group avatars with `Users` fallback.
- `src/chat/components/ChatLayout.tsx` — `h-full` root, bootstrap, debounced reload; root `<div data-chat-root data-text-size data-density style={themeVars}>` wires `--fd-accent` (user accent color), `--fd-bubble-mine-bg/text` + `--fd-bubble-other-bg/text` (falling back to `bubbleTextColor(bg)` auto-contrast when the setting is empty) for `MessageList` bubbles; loading text uses `t('loading')`.
- `app/(auth)/chat/page.tsx` — **Phase 9:** now a 1.37 kB `dynamic()` loader (`NEXT_PUBLIC_USE_NEW_CHAT` gate still active) selecting `ChatLayout` (new chat) vs `ChatPageMonolith` (legacy) — neither ships in any page chunk. Both are `next/dynamic` `ssr:false`.
- `app/(auth)/chat/layout.tsx` — **Phase 9 route layout** importing `@/chat/chat.css` (chat-only CSS served only on `/chat`).
- `src/chat/chat.css` — **Phase 9:** chat-only styles moved out of `globals.css` (typing-dot keyframes/`.typing-dot`, `.chat-list-ul/ol`, `.chat-mention`, `[data-chat-composer] .chat-spell-error*`), fixed a broken brace that had orphaned them.
- `src/chat/monolith/ChatPageMonolith.tsx` — **Phase 9:** legacy gated chat page (≈1500 lines) moved verbatim; own chunk, unused in prod.
- `app/(auth)/layout.tsx` — `min-h-0` on both overflow wrappers.
- `app/globals.css` — `.chat-spell-error` / `.chat-spell-error-active` **now scoped under `[data-chat-composer]`** (+ `.dark` variants) so the red squiggle can never render in message bubbles; the composer contenteditable is tagged `data-chat-composer` in `Composer.tsx`; `.chat-mention` now uses `var(--fd-accent)` + `--fd-accent-soft` with a dark `color-mix` variant.
- `app/color-system.css` — **design tokens (Phase 7)**: `:root`/`.dark` `--fd-*` CSS variables (bg/surface/ink/muted/border/accent + `--fd-text` scale + bubble/read-tick/reaction/reply/highlight tokens); accent utilities `.fd-accent-bg/-text/-soft-bg/-soft-border/-ring:focus-visible`; `[data-text-size='sm'|'lg']` font-scale rules (applied to `.fd-bubble-text`/composer via `--fd-text`); `[data-density='compact'] [data-message-row]` 2px-row/8px-last-row compacting; imported from `app/layout.tsx` (line 5).
- `tailwind.config.js` — **`content` now includes `./src/**/*.{js,ts,jsx,tsx,mdx}`** (root cause of the oversized-collage/arbitrary-class bug: without it `src/chat` classes emitted no CSS).
- `next.config.js` / `package.json` — `eslint.ignoreDuringBuilds`, 8GB build heap.
- `database/migrations/20260827_chat_extensions.sql` — applied to Supabase.
- `database/migrations/20260828_chat_message_reads.sql` — **applied** to Supabase (successful run; drop+recreate with permissive RLS + realtime publication; pre-existing incompatible table fixed by `7920a4a` + `08c0702`).
- `database/migrations/20260828b_chat_message_reads_replica_identity.sql` — **applied** (`REPLICA IDENTITY FULL` on `chat_messages` + `chat_message_reads`; soft-deletes + read-receipt payloads now full-row).
- `database/migrations/20260828c_fix_chat_uploads_storage.sql` — **applied** to Supabase (ensures `chat-uploads` bucket exists, accepts any mime type, and has anon/authenticated `storage.objects` policies; fixes the 500 "Failed to create bucket … row-level security" on attachments).
- `database/migrations/20260901_chat_messages_is_forwarded.sql` — **applied** (adds `is_forwarded BOOLEAN NOT NULL DEFAULT false` — "Reenviado" badge live; schema-verified).
- `src/services/chatSettingsService.ts` — settings service (real file; `services/` is a symlink): `ChatSettings`/`ChatSettingsUpdate` types + `DEFAULT_FIELDS` extended with **`text_size`/`density`/`accent_color`/`my_text_color`/`other_text_color`** (text colors default `''` = Auto → client `bubbleTextColor`).
- `src/chat/components/ChatSettingsPanel.tsx` — **Chat Settings panel**: wallpaper designs/upload/reset (per-conversation scope + global fallback) + **new Phase 7 sections** — text-size segmented (`TEXT_SIZES` sm/md/lg), density segmented (`DENSITIES` comfortable/compact), accent color swatches (`ACCENT_COLORS` — 12, applied as `--fd-accent` on the chat root), and my/other text-color swatches (Auto `''` + shades; check icon contrast via `bubbleTextColor`); all selection highlights use the accent token.
- `database/migrations/20260903_chat_settings_scoped.sql`, `20260903_chat_settings_wallpaper_style.sql`, `20260904_chat_settings_phase7.sql` — **applied + schema-verified live** (scoping pkey `id uuid` + `conversation_id` partial unique indexes; `wallpaper_style text NOT NULL DEFAULT 'classic'`; phase-7 5-column add; settings persistence working).
- `src/chat/components/ConversationListItem.tsx` / `ChatHeader.tsx` — group conversations render a gray circle with the `Users` icon when no group `avatar_url` exists.
- `src/chat/components/ChatPane.tsx` — when no conversation is selected: header + composer hidden and WhatsApp-style default panel shown (gray circle + "Tap a chat to start messaging.").
- `app/api/agent/file-access/route.ts`, `app/api/logs/route.ts`, `src/lib/file-access-log-store.ts` — file-access tracking (Phase 0/10 tooling).
- `src/types/chat.ts` — `ChatMessageRead` type + `reads` field on `ChatMessage`; `ChatAttachment` type; **`is_forwarded` on `ChatMessage` + `CreateMessageData`**.
- `public/sw.js` — **Phase 5 push worker (CACHE_NAME `diamond-link-v10`)**: off-line shell precache (`sw-precache.json`), stale-while-revalidate runtime caching, and the push pipeline — native IndexedDB thread store (`diamond-link-push/threads`, `getThread`/`saveThread`/`deleteThread`), per-thread aggregated WhatsApp-style `push` handler (`showThreadNotification`), legacy fallback `showSimpleNotification` (test/calendar/patient), **always-show never-silent guarantee**, `notificationclick` (clear thread queue → prefer open `/chat` window via `NOTIFICATION_CLICKED` postMessage → `openWindow('/chat?conv=<id>')`).
- `app/api/push/webhook/route.ts` — pg_net webhook target (secret-gated, resolves participants + sender via `clerkClient`, strips HTML, skips muted, **sends structured `data` fields** for per-thread aggregation).
- `app/api/push/send/route.ts` — client "hidden tab" fallback (authed; forwards `title`/`body`/`icon`/`tag`/`actions`/`data` to the caller's own subscriptions).
- `app/api/push/vapid-public-key/route.ts` · `subscribe` · `unsubscribe` · `test` — VAPID key + subscription lifecycle + test button.
- `src/lib/push/pushService.ts` — `PushNotificationPayload`/`NotificationAction` types, `sendPushToUser` (VAPID signing, TTL + `urgency:'high'`, dead-endpoint pruning), `sendTestNotification`.
- `src/chat/push/triggerHiddenTab.ts` — hidden-tab client trigger → `/api/push/send`.
- `src/chat/hooks/usePushNotifications.ts` — permission → `PushManager.subscribe` → server upsert lifecycle (status machine).
- `src/chat/components/ChatLayout.tsx` — SW registration, `handleIncomingMessage` hidden-tab payload (structured `data`), `NOTIFICATION_CLICKED` listener routing **Responder** (select thread + retry-focus `[data-chat-composer]`) / **Marcar leído** (`markConversationRead`) / body-tap (`setSelectedConversation`), `?conv=` `DeepLinkEffect` (idempotent-guarded), signed-out cold-boot screen.
- `src/chat/store/chatStore.ts` — **idempotent `setSelectedConversation`** (React #185 fix) + `markConversationRead`.
- `middleware.ts` — `/chat` + `/chat/*` exempted from `redirectToSignIn()` (notification taps on cold boot land on `/chat`, not sign-in).
- `database/migrations/20260906_push_subscriptions.sql` — `push_subscriptions` table + RLS (applied).
- `database/migrations/20260906b_chat_messages_push_webhook.sql` — pg_net finalize trigger on `chat_messages` INSERT (applied; `headers` named 4th arg).

---

